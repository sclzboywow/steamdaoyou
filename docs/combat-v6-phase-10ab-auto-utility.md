# Phase 10AB：通用自动战斗策略底座

> 2026-09-11；首版底座与场景接入已实现，完成共享测试和重点本地验收。10AA明确跳过，其遗留项不视为完成。本阶段未发布。

## 1. 已确认边界

- 主动AUTO、天骄榜等非实时对战副本、所有NPC敌人使用公共效用策略。所有NPC包含此前固定普攻、防御或技能轮换的敌人，以用户最后确认覆盖早期保留建议。
- 玩家手动下令、擂台超时补令、客户端3秒请求一次AUTO、离页／刷新回手动保持现状；不增加离线托管。
- 内部均衡／进攻／保守预设，线上入口默认均衡；不开放策略选择页面。
- 不自动换宠、逃跑、捕捉或用药，不做全队托管、Boss行为树、多回合搜索或数值平衡。
- 蜃楼周进度仍只保存Redis，不新增持久周进度或结算账本。

## 2. 实现职责

共享应用层 `src/shared/combat-v6`：

| 模块 | 职责 |
| --- | --- |
| auto-observation | 按控制者过滤战局；仅自身及所控灵兽精确数据，其余单位公开血蓝万分比、状态及护盾，隐藏属性按观察者基线估算 |
| auto-utility | 普攻／防御及合法技能候选，输出／生存／控制／成本评分，固定排序与有限目标组合 |
| auto-policy | 算法和参数联合版本、三种权重预设 |
| auto | 保留公共单次调用入口、已有本方指令、人物与灵兽的短期意图折扣、可选内部解释 |

评分器只接收过滤后的观察和本单位合法选项，不读取原战局指令、敌方构筑、随机数。合法选项由现有引擎查询产生，执行时仍由引擎复核。隐藏最大气血、攻防和等级不会因为服务端可读而参与评分。旧 `npcStrategies` 保留内容／快照结构，Host不再按其普攻／防御／轮换分支执行。

复用规则公式与效果条件匹配，不模拟结算或消耗随机数。有效伤害／恢复按最大气血归一；击倒、复活和状态按行动价值启发式估算；成本按法力、气血和技能资源比例归一。随机分支按概率计收益。已计划的伤害／治疗折半计入，重复控制／复活降权，不假定已经命中。

首版不完整推演被动、反击、复杂成功链和未来回合。无法估算的额外效果不计收益并写入内部说明。解释数据默认不入库、不返回前端。首版结果不代表最优战术或宗门平衡结论。

## 3. 场景与版本

公共AUTO覆盖练功房、野外、秘境、蜃楼、宗门任务、突破、擂台主动AUTO；公共PvE Host覆盖NPC及非实时对手副本；天骄榜双方先生成本回合决策，再提交执行。

战斗版本新增可选 `autoPolicyVersion`，新场次记录 `combat_auto_utility_v1`（包括预设权重）。归档旧版本字段可缺省，回放不重新决策。旧运行态版本不匹配时拒绝恢复，不静默套用新策略。

上线先在旧版本停止新开战，处理完旧活动战局和未完成榜单挑战后切换；不以删除Redis键代替结算。该维护动作未在本阶段执行，且不恢复被跳过的10AA发布基线工作。

## 4. 验收

共享测试覆盖：相同输入重复决策、快照／随机数不变、隐藏数据扰动不影响观察与评分、策略偏好区别、缺蓝／封印与无合法目标、有效治疗／复活、低效技能不强放、概率分支、意图折扣、NPC及榜单接入、恢复与差量回放。

本地页面重点验证主动AUTO、NPC实际施法、手动与刷新边界、榜单终局及回放；未实际执行的跨场景矩阵不标记通过。

## 5. 本轮验收记录

环境：本地 `127.0.0.1:5174`／API `3001`，复用本地道友5（九劫天宫、炼气后期），未发放物资或调整角色。使用内置浏览器操作正式页面。

- 攻击陪练 `d6c33bd8-ee39-4a69-b903-0930c005e5aa`：主动AUTO，人物依次使用雷霆万钧、天雷斩并选择普攻；4回合落败，正常显示终局与回放入口，已结束训练。
- 净化训练：玩家手动防御，术法陪练实际施展训练封禁，受术同伴获得训练封法；玩家指令没有被替换。第二回合开启AUTO后立即刷新，仍为原第二回合，AUTO关闭、等待手动下令。随后通过正式放弃入口结束训练。
- 天骄榜 `578264a4-ff40-46ed-ab42-0667823339bc`：本地道友5挑战本地道友1，双方实际施放输出、减益和增益技能，11回合胜利；终局HP658／MP767。刷新同一requestId、跳转末回合得到相同终局及第1名，剩余次数仍9/10。正式近期战绩入口可读取32帧回放，敌方血蓝保持百分比展示。
- 返回洞府时现实HP1550／MP902、灵气200、修为0、灵石0、声望0与测试前一致；不回滚正式挑战产生的排名、次数和战绩。未遗留本次活动训练；浏览器warn/error查询为空。

检查命令：

- `bun run test src/shared/combat-v6 src/shared/engine/combat-v6`：中途375项通过；追加资源蓄积边界后由全量测试覆盖。
- `bun run test`：最终147个文件、997项通过。
- `bunx tsc -b tsconfig.app.json tsconfig.node.json --pretty false`：通过；最终由build再次覆盖两端类型检查。
- `bun run lint`、`bun run build`、`git diff --check`：最终均通过。构建既有大chunk提示不在本阶段调整范围。
- 对新增模块及实际改动块检查格式；代码全部通过内置apply_patch修改，未使用Python改写或格式工具写回。

剩余边界：本轮没有逐场跑野外／秘境／蜃楼／宗门／突破、真实4v4带宠和独立观众矩阵；这些入口完成代码接入与共享Host回归，不等同所有运行场景验收。未执行PG／Redis／消息故障、多实例及目标环境版本切换；未做全宗门胜率、全部技能估值校准、真机与Profiler专项。复杂被动、条件成功链、引擎自选目标及未来行动仍是启发式估算，后续依据可复现局面改进，不据本轮结果修改数值。
