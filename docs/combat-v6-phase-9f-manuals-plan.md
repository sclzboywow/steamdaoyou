# Phase 9F：功法玉简与人物道印养成闭环

> 历史记录：2026-09-10 起采用[境界功法层数养成](./combat-v6-manual-system-design.md)，本文旧操作及验收不代表新方案已验收。

> 2026-09-07：实现完成，核心本地链路已验收。未验证项单独列于文末。

## 确认范围

- 沿用 Phase 5A 十谱系、本篇／真解二十定义及境界派生的 2～6 道印位，单构筑。
- 玉简进入唯一库存；固定定义堆叠键，每格 99。只消费储物袋中的玉简，储藏室需先取出。
- 参悟、指定改修各消耗一本，无额外资源、冷却或失败概率。真解可直接参悟，同源本篇只能原位升级；重复与降级拒绝。
- 替换目标本身的冲突可移除，其他位置冲突须先散功。免费散功，无返还。
- 野外按胜利场次独立 3% 产出一本，十谱系等权，本篇总权重 95%、真解 5%；概率不对外显示。不赠送初始玉简。
- 悟道功法页面硬切新版；旧创建及确认写入关闭，保留旧数据及放弃待确认结果的清理能力。
- 无 AI、随机属性、升级经验、多构筑、残页、交易、药品、旧数据转换或全仓 v5 删除。

## 实施方案

1. 玉简定义分类注册，引用现有功法 ID；业务掉落池升版接入，通用底座不变。
2. 复用现有功法状态和槽位表；角色 Redis 锁、SQL 角色锁、revision 与同一事务完成消费和构筑更新，不新增凭据表。
3. 新页面主展示道印位，统一抽屉选择与确认、tooltip 说明；库存参悟链接进入同一流程。
4. 活动战斗及结算期间禁止修改。开战冻结构筑、下一场更新、既有回放不变。
5. 纯共享规则测试、lint/build、触及文件 Prettier 和本地真实页面验证；历史暂缓项不代替本轮通过记录。

## 发布

无需新增数据库迁移。前后端同步发布并刷新旧页面。青溪掉落池版本由 1 升至 2，已开始的战斗沿用冻结配置；新战斗采用新版。正式概率尚不代表经济平衡已验证。

## 验收记录

- `bun run lint` 通过；`bun run build` 客户端、服务端及 resolver Worker 通过，保留既有大 chunk 提示。触及文件 Prettier、`git diff --check` 通过。
- 聚焦功法／玉简／掉落测试 3 文件、19 项通过；全量共享测试 217 文件、1997 项通过。没有新增前端、服务端或数据库单元测试。
- 本地 `127.0.0.1:5174`，API `3001`，专用 PostgreSQL `15432`／Redis `16379`；复用本地道友2。验证本篇参悟、原位升真解、真解直接参悟、同源禁用、指定改修及无返还散功。每次参悟／改修只扣一本，零灵石、满灵气仍可完成。
- 改修确认双击只消费一次，随后重放旧请求返回 409，玉简数量和 revision 未再次变化。战斗中直接请求散功返回 409；储藏室玉简不进入可用列表，直接消费也返回 409。匿名读取返回 401，旧创建及确认接口返回 410。
- 物品栏分类、玉简详情、存取、参悟链接进入同一抽屉流程通过。桌面及 360px 抽屉完成视觉检查，控制台未发现错误。
- 真实野外战斗 `6d0a2e7a-7d84-4afe-8ca6-8e8309b5c52f` 中，《冥思真解》使人物施法消耗 20 MP 后在回合末恢复满 MP。胜利获得一本《断岳诀》玉简，与原有一本合并为一格两本。
- 掉落验收仅临时将本地玉简组概率设为 1，开场冻结后恢复正式 0.03 并重启 API。战斗仍按冻结配置结算；提交代码没有临时概率或功能开关。本轮没有将该确定性验收当作正式概率经济抽样。
- 散功前后，本次回放摘要保持 `570670b05c07ae9b15792f91887297c7`；此前 9E 回放 `803488d2-1ccb-4df4-8aa0-3b0a23977775` 摘要保持 `6e52d27ece71fcdbef6c629f7abba4c6`。已结束活动战斗、通过页面清空本轮道印，清理三格剩余测试物资；保留正常战斗进度与归档，未重置环境。

未验证：正式概率长期经济抽样、真实满包玉简溢出、多进程并发与进程中断／网络丢响应故障矩阵，以及完整 PvP／观战回归。旧 revision 拒绝和刷新恢复已验证，但不替代上述故障验收。既有阶段暂缓项继续独立保留。
