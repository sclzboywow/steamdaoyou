# 变异灵兽

确认日期：2026-09-19。变异是同一物种的个体特征，沿用物种 ID、栖息地和培养规则。

2026-09-20已接入[宝宝体系与融合](combat-v6-beast-fusion.md)。变异宝宝0级五项基础各20、另有50自由点，总计150；不允许融合。野外变异统一生成0级宝宝，每只0.8%的概率及资质成长10%提升保留。

## 规则

- 野外每只个体独立以 0.8% 概率变异，抽中后统一为0级宝宝。概率来自 `wild/data/wild.json` 的 `encounter.mutantChance`，当前格式版本 4、内容修订 8。
- 遭遇生成时确定变异，野外发现预览显示异色及“变异”，战斗内仅显示异色。开战、重连、捕捉与结算使用同一份个体事实，不再抽签。初始领取只生成普通个体。
- 五项资质先按物种原范围抽取，再乘 1.10，四舍五入为整数。成长先按原千分位范围抽取，再乘 1.10，四舍五入到三位小数。数值提升仅应用一次。
- 技能池、必带技能、数量概率、技能格、寿命、升级加点、携带要求和捕捉公式沿用普通规则。变异不保底满技能；无必带物种仍可生成零技能个体。
- 洗炼保留变异身份，以物种基础范围重抽后再应用一次提升；普通个体不能洗成变异。药露种类及消耗不变。传承仍是零技能开启第一格，已有技能随机覆盖。
- 变异资质分布整体提高，但某项低资质变异仍可能低于优秀普通个体。不另设综合品质、隐藏保底或合宠规则。

## 数值与存量

面板的等级资质项与属性成长项相加；资质和成长只强化一次，同时变异基础属性由各10提高到各20。最终面板不能再概括为约提高10%，伤害和胜率也不承诺同比变化。

`BeastSchema.isMutant`为可选布尔值，缺失按普通处理；`originKind`与`initialLevel`是必填事实。变异只能与宝宝身份组合。不为未发布的旧个体做字段回填。保存最终资质与成长，投影只根据变异调整基础属性，不重复追加资质／成长倍率。

## 展示

`BeastPortrait` 复用 `GameIcon` 的注册头像或 emoji，以 CSS 的 `sepia / saturate / hue-rotate` 组合生成紫色异色。滤镜只作用于头像，不改变血条、文字或技能；透明度与轮廓保留。野外发现与灵兽袋使用共用“变异”标签；战斗编组、单位详情、召唤选择及回放只通过头像变色区分，不显示“变异”文字或标记。

接入野外遭遇、灵兽列表与详情、战斗编组、单位详情、召唤选择与回放。洗炼确认层提示保留变异。CSS 是整体调色，不提供毛发、眼睛、火焰的分区着色。

## 验证

- 新增纯共享领域测试覆盖全部 18 物种的数值提升、技能独立、普通兼容、面板只强化一次、重复洗炼、传承保留身份、概率边界、幼崽／成年变异、预览边界、身份一致性、战局恢复及回放归档。
- 普通个体的既有生成摘要测试保持原值。
- `bun run lint`、`bun run build` 通过。全量 `bun run test`：1,584 通过，1 项既有失败为 `src/shared/lib/bodyCultivation/pack.test.ts` 的 `shortDesc` 文案基准，与本机制无关。
- 最终聚焦测试 `bun run test src/shared/engine/combat-v6/beasts src/shared/engine/combat-v6/wild src/shared/combat-v6`：28 个文件、408 项通过。

### 首版的本地浏览器验收（0.1%）

环境为 `localhost:5174` / `127.0.0.1:3001`，使用既有本地测试角色。仅准备遭遇期间把本地变异概率临时设为 1，随后立即恢复 0.001；没有新增调试开关或改变最终概率。移动端检查使用 360px，完成后已恢复浏览器默认视口。

- 青溪坡生成三只变异个体，包括 emoji 烛尾狐与透明 WebP 咪咪；桌面与 360px 的异色、变异标签正常。移动端的等级／成年文字改为整项换行，避免被挤成竖排。
- 恢复概率并重启服务后，既有遭遇仍保留相同变异身份。开战、捕捉目标选择与敌方头像一致，捕捉按原概率执行，存在正常失败与成功。
- 捕获变异烛尾狐及咪咪两只，读取遭遇快照和数据库确认入库时的全部个体事实完全一致。
- 用户随后接手页面并连续洗炼咪咪。只读核对洗炼后的种子与领域生成结果：仍为变异，资质与成长精确对应一次 10% 提升，没有重复累乘；用户又自行改名，变异身份保持。
- 原有 19 只灵兽个体事实保持不变。按用户明确要求保留本次捕获的两只灵兽及其后续培养结果，不清理用户正在使用的状态。验收时曾取消原首发、发放两瓶普通归元灵露；后续由用户继续操作，未擅自回退这些状态。
- 控制台未发现游戏代码错误；记录到密码管理器浏览器扩展的 `insertBefore` 错误，来源为扩展脚本。
- 回放字段兼容与外观保留通过共享契约、恢复及回放单测验证；未另行进行竞技场召唤和历史回放页面的浏览器验收。野外沿用原有回放保留策略，本次没有新增保存策略。

当次日志及只读核对记录位于 `/tmp/daoyou-mutation-*.log` 与 `/tmp/daoyou-mutation-*.json`。

### 后续调整

按用户要求，出现率从 0.1% 提高到 0.8%（每只独立，内容修订 8）；移除所有战斗展示组件及其无障碍名称中的“变异”标识，保留异色。原始外观事实继续随战局和回放保存，数值、技能及洗炼规则不变。野外发现、灵兽袋列表和详情仍保留标签。

本次调整验收：`bun run test src/shared/engine/combat-v6/wild src/shared/combat-v6` 的 12 个文件、92 项通过；`bun run lint`、`bun run build` 和 `git diff --check` 通过。代码检查确认三个战斗展示组件仅将变异身份传给头像，没有变异标签或变异无障碍名称。当前浏览器处于登录页，本次未重新开战手测，也未重跑全量测试；未操作用户保留的两只灵兽。
