# 管理后台新版物品接入

万界商行、宗门宝库、游戏邮件和兑换码统一复用新版物品栏的 `ItemGrant`、物品定义注册表和实例事实校验。管理后台不再维护一份独立物品协议。

## 配置与交互

- 四个入口共用 `RewardItemPicker`，使用现有 `InventoryGrid`、`ItemSlot` 和物品预览；可搜索、分类、翻页、预览后选择。
- 注册表提供图纸、传承灵印、灵兽培养道具、功法玉简和阵纹；材料库提供 V1 材料及有效灵种。
- 道装、丹药、灵果、符箓通过当前领域生成器生成，预览确认后保存完整事实。消耗品直接写入新版库存奖励，不再创建旧版消耗品库条目。
- 两个玩家商店同步复用物品栏格子与预览，保留价格、每周限购、购买和资源刷新流程。
- 游戏邮件及兑换码保留灵石、声望奖励；系统邮件发布前展示条件、时间与奖励摘要，不再预估收件人。参见 [系统邮件按条件投递](system-mail-campaigns.md)。
- 原道具库改为材料库，删除旧法宝／消耗品编辑与选择入口。保留材料及灵种生成和每日生成设置；不支持的旧材料只显示弃用状态。

## 保存与发放

商店的 `item_snapshot` 保存不含数量的 `ItemGrant`，数量继续使用既有列。邮件和兑换码保存既有 `inventory_v1` 附件快照。每次购买、每位邮件收件人和每次兑换分别生成新的道装实例 ID，其他已确定属性与技能保持不变。

发放继续经过现有事务、库存、邮件领取和资源更新流程。历史邮件及兑换码保留原附件协议；旧附件解析只服务于历史读取和领取，不再用于新配置。

## 发布与迁移

发布本次应用代码前，需要执行业务迁移 [0054_admin_inventory_rewards.sql](../drizzle/0054_admin_inventory_rewards.sql)，并避免旧版本在迁移后继续配置商品。迁移不删除历史表或购买记录。

1. 为两类商店增加物品快照；旧材料库来源改为可空，移除来源唯一索引，支持同一物品配置多条商品。
2. 对能够无损转换的普通 V1 材料回填快照，保留商品 ID、价格、数量与购买历史，包括旧功法典籍／神通秘术材料分类。
3. 其余旧商品（包括旧法宝、旧消耗品及未回填的灵种）下架。管理员从“重新配置”选择新版道具后再上架，不自动猜测替代品。
4. 旧法宝／消耗品材料库条目归档，历史奖励快照保留。

本轮仅对 `env/local.env` 对应的本地数据库执行迁移。使用明确加载环境的命令：

```bash
bun --env-file=env/local.env node_modules/drizzle-kit/bin.cjs migrate
```

## 本地验收（2026-09-23）

环境：前端 `127.0.0.1:5174`，API `127.0.0.1:3001`，数据库 `127.0.0.1:15432/daoyou_local`。

- 普通用户无法进入后台；管理员可生成、预览及上架新版道具。
- 万界商行丹药连续购买两次，扣除声望、入库与每周限购正确。
- 宗门宝库道装连续购买两次，扣除贡献、限购正确；只读核对两个实例 ID 不同，属性与商品快照一致。准备测试时临时调整的宗门身份已恢复。
- 指定本地角色收到阵纹奖励邮件，成功领取；背包满时按既有规则转入储藏室。
- 同一个道装兑换码由两个角色成功兑换，附件属性相同、实例 ID 不同；其中一个角色完成邮件领取。
- 桌面及 360px 物品预览可用，器诀展开保持浮层，Esc 关闭正常；浏览器控制台无错误。
- 验收商品已下架，兑换码已停用；实际玩法资产和领取历史保留。

静态及共享逻辑检查：`bun run lint`、`bun run build`、`bun run test src/shared/contracts src/shared/inventory src/shared/items src/shared/lib`、`git diff --check`。

未执行全仓战斗单测、LLM 材料／灵种批量生成的实际调用或预发布／生产部署。本轮浏览器验收覆盖上述代表性道具；全部固定物品定义的奖励校验由共享单测覆盖。

## 后台交互重构（2026-09-23）

本轮重整五个页面的任务流程。系统邮件随后改为按条件异步投递，以下表格反映当前入口；物品奖励协议保持复用。

| 页面 | 首页 | 操作流程 |
| --- | --- | --- |
| 材料库 | 搜索、分类、发布状态与物品格列表 | 新增／编辑打开独立窗口；批量生成与每日设置分别打开，生成时只展示当前所选类型的参数 |
| 万界商行、宗门宝库 | 商品列表显示物品格、价格、限购和上架状态 | 新增／编辑打开窗口，选择道具后配置商品；保存后关闭并刷新列表 |
| 兑换码 | 搜索、状态、有效期与领取名额 | 奖励详情按需打开；新建按“奖励与邮件 → 领取设置 → 核对创建”，成功页保留可复制的兑换码 |
| 系统邮件 | 发布记录列表、状态与实际投递数 | “内容与奖励 → 条件与时间 → 核对发布”；支持草稿、复制和停用，角色活跃时异步投递 |

道具选择器按真实物品分类显示，所有候选均使用 `ItemSlot`。丹药、灵果、符箓和道装的生成参数进入独立步骤；生成后回到候选物品格，通过与其他类型相同的预览操作选择。材料按具体种类向服务器查询，避免先分页再排除灵种造成空页。

奖励编辑区以物品格展示已选道具，在预览中调整数量或移除；货币保留各自数量输入。后台使用局部原生模态窗口管理焦点与滚动，Esc 只关闭当前层；校验错误在当前窗口内显示。移动端导航默认折叠，编辑窗口正文滚动、操作区固定可见。

本轮本地验收覆盖：

- 五个页面的桌面及 360px 布局，无横向溢出；所有道具类别的物品格，生成后返回列表、预览、选择与数量编辑。
- 材料新增后回列表、搜索及再次编辑；测试材料保持归档。商品保存后回列表，测试商品保持下架。
- 兑换码返回上一步保留草稿、核对创建、列表搜索与详情；测试兑换码 `LOCALUI0923` 已停用。
- 邮件必填校验、指定角色预览、奖励核对及实际发送；仅发往本地测试角色。
- 窗口及预览分层关闭，关闭后恢复页面滚动；窗口内错误提示可见，浏览器控制台无错误。

检查命令：`bun run lint`、`bun run build`、`git diff --check`。本轮为前端 UI 与交互调整，未新增或运行单元测试，未调用 LLM 批量生成，未操作预发布或生产环境。
