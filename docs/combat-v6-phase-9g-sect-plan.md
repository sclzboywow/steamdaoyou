# Phase 9G：宗门心法与经脉养成闭环

> 2026-09-07：代码已落地，本地养成主链路通过。以下未验收项不计为通过，既有暂缓验收保持独立记录。

## 边界与规则

- 已接入五宗门硬切 v6 心法、经脉和技能页面及写入接口；宗门社会玩法不重做。
- 六心法逐级升级，最高 min(180, 人物等级+10)，分支不超过主心法。目标等级 L 的修为为 50×1.05^(L−1) 向上取整到10，灵石为修为的3倍向上取整到100，不消耗感悟。
- 经脉七层人物门槛为30、60、90、120、150、170、180。修为为5000、20000、80000、320000、1280000、5120000、5120000，灵石为修为5倍，每层感悟100。逐层付费一次，两流派共用深度，不增设心法门槛。
- 每层至多一节点，允许空位、不完整方案；战斗外保存和切换免费、无冷却。草稿离开或切换需保存或放弃，不静默丢失。
- 活动战斗及结算期间禁止调整，下一场生效；回放不修改。
- 转宗六心法按槽位继承、深度保留，目标节点清空，流派按现有转宗方向映射。道印、装备和修炼保持不变。
- 不新增宗门内容、AI、药品、阵法、全站重构或新消费凭据表。

## 数据迁移

一次性 SQL 对当前有效 membership 的旧解锁层 ID 映射层号，跨流派取最大值，不求和。最终与现有 v6 深度取大，不降低进度、不覆盖新版节点、不重新收费、不返还历史投入；旧终式映射第六层，第七层不赠送。非法旧层 ID 显式报错，不静默跳过。

转宗复用并改绑同一构筑 profile，避免复制道印和装配状态；历史目标 profile 不作为第二份养成权威。SQL 迁移先完成，再同步发布前后端并刷新旧页面，关闭旧战斗养成写入，不新增功能开关。

## 验收

本次环境为 APP_ENV=local：页面 127.0.0.1:5174、API 3001、PostgreSQL 15432。未写入预发布或生产。

- 共享测试：218 个文件、2003 项通过；新增 6 项纯规则测试覆盖费用、上下限、七层门槛、空方案、非法节点、五宗门双向转宗槽位映射及技能目录。
- lint、客户端／服务端／resolver Worker 构建、触及文件 Prettier、git diff --check 通过。构建仍有既有大 chunk 提示。
- 真实认证页面：六心法目录及详情，1→2 级准确扣 60 修为和 200 灵石；并发同 revision 升级一次成功、另一次429，无重复扣费。
- 首层解锁准确扣 5000 修为、25000 灵石、100 感悟；节点保存回显、免费切换流派、切换／跨页离开的草稿保护通过。模拟另一请求更新 revision 后，保存返回409，页面保留草稿，明确提示重新读取会放弃草稿。
- 资源不足、旧 revision、非法节点均返回409；真实创建练功房会话后，养成读取显示占用原因，写入返回409，随后关闭本次练功房会话。
- 360px 经脉三列布局无横向溢出，节点说明复用独立 Tooltip；桌面心法详情与经脉页面可操作。
- 使用本地道友2临时等级与资源验收，完成后恢复原境界、资源、主心法1级、经脉深度0、空节点和原勾魂阎罗流派；revision 保持单调，不回退。临时进度最初缺少既有必填字段导致角色资源读取500，补全验收数据后恢复正常，此项未修改产品代码。

### SQL 验收与发布

- 0042 仅为数据迁移，生成快照与0041的业务 schema 语义一致。
- 本地正式迁移前后均为8个深度0档案：本地没有旧经脉行，因此该分布不能证明历史继承。
- 另在本地事务中插入旧两流派层级样本，执行完整0042 SQL：旧层级最高6映射为6，重复执行 revision 不再增加，已有新版7保持7；全部事务回滚，不保留样本。
- 标准 `bun run db:migrate` 的 auth 步骤成功，但后续 `bun x` 未传递 DATABASE_URL。业务迁移使用 `bun --env-file=env/local.env node_modules/drizzle-kit/bin.cjs migrate` 成功。部署时需显式向 Drizzle 进程注入目标环境变量，不直接复用 local.env。
- 发布维护期先备份并执行0042，再同步发布前后端、刷新旧页面。旧心法／经脉／四技能栏／自动战术写入口返回410，不兼容旧页面提交。

### 仍待验收

- 真实认证转宗携带非空道印／装备及转回历史玉牒链路，已由下述符箓适配验收补齐。
- 高层付费解锁逐层浏览器验收、完整多人战与回放回归。本次规则测试覆盖全部七层，运行态只执行第一层及练功房占用拒绝。

### 符箓 v6 适配补充验收（2026-09-07）

- 欺天符删除旧养成读取、迁移、清空、旧神通卸装及旧 progression 事件；只迁移 v6 构筑并更新宗门身份与既有任务／俸禄归属。响应提供 membership，不再提供旧 sect 战斗状态。
- 洗脉符改为清空 v6 两流派节点，保持深度和当前流派，增加方案与构筑 revision。空方案不扣符，节点重置和扣符同事务；平时免费调整节点规则不变。两符箓库存与发放不迁移。
- 本地道友2：临时两符箓各3张，深度1、两流派各一个节点、断岳普通道印及10级武器。洗脉一次返回200，清空两套节点；再次返回400且符箓剩2张，深度、流派、道印及装备保留，返回 v6 构筑失效事件。
- 通过页面确认幽都→红尘剑宗，随后认证请求反向映射转回幽都，并重试相同幂等键：均成功响应，欺天符只消耗2张，共用深度1、六心法等级、同一 profile ID、道印与装备实例保持，目标节点为空。原幽都玉牒复用。
- 创建真实练功房会话后，洗脉与转宗均被战斗占用拒绝；会话已关闭，符箓数量未再变化。四张旧养成表均未新增记录，转宗代码不再访问它们。
- 临时符箓、装备、道印、节点深度及新建目标玉牒已清理，角色恢复原幽都构筑；revision 不回退。资源事件与幂等请求记录保留正常审计轨迹。
- 本轮 lint、build、9项聚焦共享测试、触及文件 Prettier 与 diff 检查通过。不新增数据库、服务端或前端单元测试；未重复全量2003项测试。无需新增数据库迁移。

### 心法与流派页面重设计（2026-09-07）

- 心法改为六卷目录与详情联动，桌面左右分栏、手机三列两行目录；展示当前／下一级面板贡献、关联神通说明与研习费用。连续研习保留当前心法和神通选择，成功使用现有轻提示。
- 经脉改为七层三列节点与详情联动；点击先预览，再选择或取消。连线只表达相邻层的已选节点，不引入前置关系。桌面内联详情，手机使用 `InkDetailDrawer`；待保存修改、切换与离开保护沿用原规则。
- 使用现有纸色、墨色与朱红色，保留 NPC 房间、HUD 和底部导航；只修改 v6 展示组件，不调整费用、养成规则、接口或持久化结构。节点与神通使用现有内容定义。
- 本地认证浏览器验收：主心法连续 1→2→3 级，每次扣 60 修为与 200 灵石，所选被动神通保持，贡献从 +2→+4→+6；三层节点保存后保留焦点并正确回显，取消节点后切换流派触发草稿保护，确认放弃后可切换。
- 桌面与 360px 窄屏检查通过；手机节点详情以独立抽屉展示，选择后收起，无重复标题。修复吸底操作栏遮挡神通说明的问题，操作栏随正文排列；心法页面横向宽度与视口均为 360px。
- 本地道友2临时资源、心法等级、经脉深度及节点已恢复原值，revision 保持单调，资源审计记录保留。
- `bun run lint`、`bun run build`、`bun run test src/shared/engine/combat-v6/sect-progression`（6项）、四个触及源码文件 Prettier 检查通过。未重复全量共享测试、跨页离开／冲突恢复和完整战斗／转宗链路；这些链路此前的验收记录及未完成项不因本次 UI 验收而改变。

### 页面细节修订（2026-09-08）

- 删除 `SectStudy.module.css`，三个 v6 组件直接使用 Tailwind v4 工具类；沿用项目色彩 token，收敛结构边框与间距，补充按钮 hover、按下、键盘焦点及减少动态效果偏好支持。
- 删除心法目录装饰序号与经脉树左侧层号，节点保持七层三列，连线坐标同步调整；层级仍在节点详情和无障碍名称中提供。
- 心法关联神通排除流派／经脉附加的被动，保留心法原生能力和经脉授予的主动神通；过滤仅作用于心法页面，不改变战斗编译或其他技能目录。
- 本地页面核对：幽都主心法不再显示勾魂阎罗、毒判等附加被动，十殿终审保留并显示第七层节点解锁要求。360px 经脉树无横向溢出，锁定节点仍可打开独立详情抽屉。此轮不修改本地角色资源或进度。
- 桌面布局、`bun run lint`、`bun run build`、三个触及组件 Prettier 及 `git diff --check` 通过。本轮仅调整前端展示，未重复共享规则测试及写入／战斗链路验收。

### 视觉风格收敛（2026-09-08）

- 研习、保存、放弃修改、启用流派和节点选择／取消统一复用 `InkButton`，保留原 pending、禁用条件与事件处理；不再使用色块主操作按钮。
- 移除本轮页面自定义圆角、信息标签底色和贡献面板底纹；神通切换使用文字与细下划线。心法册、经脉节点保留专用选中反馈，继续使用 Tailwind v4。
- lint、build、两个触及组件 Prettier 与 diff 检查通过。浏览器访问本地 5174 返回连接拒绝，本轮未完成运行态视觉复查，未重复规则与写入链路测试。
