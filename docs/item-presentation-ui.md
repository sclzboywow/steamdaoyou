# 新版物品展示

当前展示标准以 [物品预览 skill](../.agents/skills/daoyou-item-preview/SKILL.md) 及其分类基线为准。本文的日期小节保留历史实现和验收过程，其中旧操作、字段与字体方案不代表当前要求，不应据此恢复已删除的设计。

## 旧宝库联通与退役边界（2026-09-15）

- 功法典籍 `gongfa_manual`、神通秘术 `skill_manual` 继续作为材料保留，宝库取出转换为 `material.v1`，保留名称、分类、品质、五行、描述和数量；允许普通材料堆叠，不恢复旧抽取，不赋予炼丹或炼器用途。
- 未鉴定材料、缺少有效生长数据的历史灵种硬弃用，列表标记不可取出，服务端拒绝迁移。鉴定执行链已删除，旧鉴定接口返回 410；不再揭示或补齐历史事实。旧法宝不接入新版。
- 洞府宝库左侧保留历史文字列表和取出数量选择，桌面右侧使用公共物品格与预览；背包使用统一资源订阅，取出后自动刷新。移动端使用物品栏抽屉，取出成功后打开查看。旧宝库不构建物品格兼容数据。
- 本地“本地道友1”实测两种秘籍取出：旧记录数量各减1，新记录均为 `material.v1` 且保留分类与描述；桌面右栏立即更新，360px 抽屉内预览正常，页面 scrollWidth 为360。临时旧／新库存各2条已清理，视口已恢复，未做生产数据清理或批量迁移。
- `bun run lint`、`bun run build`、`git diff --check` 通过；针对物品、背包、炼器、灵种与退役边界的39项测试通过。全量 `bun run test` 为1361通过、1失败：未修改的 `skill-learning.test.ts` 宗门配置哈希断言不匹配。未实测满包拒绝、异常历史材料接口请求和真机触屏。

## 组件职责

- `components/feature/items/ItemSlot.tsx`：统一方格、底部单行省略名称、emoji、堆叠数量、已装备／已投入标记，以及浮层预览。`InventoryGrid` 只负责布局，容量和格位来自业务数据。
- `components/feature/items/presentation/types.ts`：独立展示契约。头部由字段、数量、状态条目组成；正文由普通行与单层展开项组成。展开项不能继续嵌套，折叠分组只能容纳普通行。
- `components/feature/items/presentation/registry.ts`：八类道具的静态适配器表，使用 `satisfies Record<ItemDefinition['kind'], ItemAdapter>` 检查类型覆盖。
- `components/feature/items/presentation/{basic,equipment,consumable}.ts`：各类型一次读取自身事实，提供轻量 `summary` 和延迟调用的 `preview(options)`。每类明确提供完整有序的头部、分组、描述，装备适配器负责数量省略、已穿戴状态及比较。不得先建立全类型默认字段再逐类删除。
- `components/feature/items/itemPresentation.ts`：兼容物品格、聊天摘要和拍卖货架的轻量入口，不构建药效、比较或正文。
- `components/feature/items/itemPreviewModel.ts`：仅负责解析适配器并组装标题、图标、颜色与内容，不包含类型分支。
- `components/feature/items/ItemPreview.tsx`：兼容现有调用参数的接入层，传递查看上下文、数量标签和比较对象；操作按钮仍由业务调用方提供。
- `components/feature/items/ItemPreviewView.tsx`：仅消费展示模型，负责统一排版、颜色与展开交互，不读取物品定义或判断道具类型。浮层定位和模态壳仍由调用方负责。

### 接入新类型

1. 在适配器中读取该类型的现行事实，返回图标、颜色、类型、分级等轻量摘要；详细药效和比较必须留在 `preview` 中。
2. 明确列出头部字段顺序，需要数量时使用 `quantity`，没有意义的字段不要生成；不存在“默认显示类型／资料分组”的通用规则。
3. 返回效果分组及独立描述。普通说明与数值使用 `line`；按需展开的能力使用 `disclosure`。仅允许既定层级，不传入任意 JSX 正文。
4. 注册到类型表；新增同一类型的物品通常只需要共享物品定义，无需增加适配器。公共渲染组件无需修改。
5. 检查现有库存、货架、聊天与详情入口，以及桌面／360px、折叠、空分组和比较行为。

消耗品分组通过 `role`（effect／preview／restriction／source）和 `collapsible` 声明展示语义，不根据 key 的子串或文案内容推断。已移除的默认限制不再由上游生成。符箓使用结构化 `talismanDetailRows`，预览直接消费行，使用确认文本再由相同行格式化，不反向切割说明字符串。

## 分级与交互

道装按原始 `equipmentLevel` 换算境界颜色；轻灵等器蕴不改变颜色。材料按品质，图纸按等级对应境界，传承灵印按普通／上品。名称底部单行省略，数量为 1 不显示角标，浮层展示完整名称；需要数量的类型显示精确数量，装备仅保留已穿戴状态。

PC 悬停延迟 200ms 展示，允许移入预览操作；触屏点击展示同一个浮层。采用原生 `popover=auto` 顶层展示，自动关闭其他物品预览，避让视口边缘，长内容内部滚动；不使用详情抽屉承载预览。仅打开的预览挂载正文和业务操作。

储物袋保留装备、转存、拆分和整理；道具使用仅承载无需复杂选择的直接消耗品，不在此实现功法参悟、兽诀打书等玩法。功法参悟由参悟页面承载，兽诀选择与学习由灵兽详情承载。操作成功、失败统一使用 toast，不在物品栏固定位置展示消息。炼器选材反馈同样使用 toast；物品用途、不可投入原因等决策信息仍保留在预览内。

炼器 PC 点击直接投入／移出，触屏先预览后确认；空材料位直接打开选材入口。开炉消耗由 InkModal 确认。成器动效结束后通过成功 toast 提示，成品保留在炉心；悬停或点击直接使用 ItemSlot 的统一道装浮层，不自动打开详情，也不提供再次打开详情弹窗的按钮。

## 本轮验证（2026-09-08）

本地真实储物袋已检查长名称截断、完整预览与操作区；360px 视口实测页面宽度与 scrollWidth 均为 360，预览横向范围为 8–328px。临时发放的六件库存记录已精确删除，保留其他任务物资。

炼器触屏、抽屉内浮层和本轮成器全过程尚未完成真实联调：期间并行任务修改服务端，出现 TaskApplicationService 缺少 executeTaskChallengeCommand 导出，API 停止。不能将静态检查视为这些链路验收通过。

## 并行开发完成后的复验（2026-09-08）

本轮使用本地 `127.0.0.1:5174` / API `3001`，账号本地道友1；上述构建阻断已解除。

- 储物袋：材料 12 拆为 10+2，再合并为 12；装备／卸下、玉简存入储藏室／取回、整理和搜索均通过真实页面操作。兽诀效果与灵兽选择表单正常，本轮未消费兽诀覆盖已有灵兽技能，也未消费玉简参悟。
- 桌面：悬停预览、完整名称、不同类型正文、操作区和 Esc 关闭正常；检查页面无运行时错误。
- 直接调用实际展示模块核对 30 级道装添加轻灵前后均为 `text-tier-ling`，分级仍为筑基；该检查未修改持久化道装。
- 360px：页面与 scrollWidth 均为 360；抽屉上方物品浮层范围 8–328px，同时仅一个物品预览。触屏指针事件模拟验证点击先预览、确认后投入、移出，投入后保留物品栏抽屉。浏览器不支持原生触摸手势注入，本轮不计作真机触摸验证。
- 修复窄屏炉中格子由固定内容高度撑成约 58×82px 的问题，复测六格均约 57.76×57.76px；名称仍最多两行，最小字号 10px。
- 三次真实铸造成功：两次普通动态效果，一次减少动态效果；共消耗 3 张图纸、3 份材料、300 灵石、21 灵气，生成 3 件成品。普通模式捕获两个已投入位置的 `forge-gather`、`forge-fire`、`forge-reveal`，聚光约 800ms、成器约 1100ms。成器结束自动打开详情，关闭后保留炉心成品，继续铸造清除成品。
- 单次成功开炉网络记录为一次 POST、一次后续 GET。离线提交没有生成成品或扣费，恢复网络后「重新核对」可恢复备料，随后成功铸造；修复后续库存读取错误覆盖开炉恢复说明的问题。
- `bun run lint`、`bun run build`、触及文件 Prettier、`git diff --check` 通过；`bun run test src/shared/inventory src/shared/forging` 共 15 项通过。没有新增前端／服务端测试文件，未运行全仓共享测试。

测试剩余的七条临时库存已精确删除；拆分临时条目已合并、图纸已消耗完。灵石、灵气及灵气刷新时间恢复至测试前基准，保留四条原有储藏室材料和其他角色进度。网络、动态效果、触屏与视口模拟已恢复。

## 物品格视觉收敛（2026-09-08）

图标扩大为主要视觉中心；堆叠数字采用左上角等宽半粗字体与乘号，状态标记移至右上角。名称改为底部单行省略，空格标记独立居中，不再被空名称占位挤偏。炉心成品宽度由炉阵的 25.2% 缩为 18%，略小于外围 19% 的材料格。

后续微调：数量明确使用局部系统等宽字体栈，绕开项目 `font-mono` 的文楷映射；名称底部留白改为格高 8%。炉心成品最终与外围统一为炉阵宽度 19%。无需材料格使用虚线、斜纹底与横线，未选图纸时显示「待选图纸」；可投入格使用朱色实线与加号。

使用已有材料检查桌面与 360px：窄屏格子为 70×70px，空格标记容器与格子中心重合，页面无横向溢出。本次仅修改视觉布局，未重复消耗物资开炉。

## 操作职责收敛（2026-09-09）

物品栏移除参悟与打书操作，兽诀学习迁入灵兽详情的独立选择界面，复用既有学习接口。储物袋成功、失败与移动指引改为 toast，保留刷新与取消移动控件；炼器物品栏不再保留固定选择消息区域。

本地页面验证移动指引 toast、取消移动、灵兽详情进入学习界面及无兽诀空态。360px 下学习界面无横向溢出。当前角色背包没有兽诀，本轮未验证消耗兽诀、随机覆盖、失败恢复及炼器选择 toast；未改动用户自测物资。lint、完整 build（含类型检查）通过；仅涉及前端交互，未新增或运行共享单元测试。

### 炼器确认与成品预览复验

本地角色完成一次真实铸造：消耗 1 张 10 级法衣图纸、1 份自测玄铁、100 灵石、7 天地灵气，生成青石法衣并保留在储物袋。确认层为 InkModal；动效结束未自动打开详情，炉心点击直接出现统一道装预览，已无「查看详细效果」按钮。360px 下预览宽 320px、页面无横向溢出。成功 toast 的动画结束回调已核对，但自动化读取晚于短暂提示，未捕获该条 toast；未另做真机触摸或减少动态效果复验。lint、build、Prettier 通过，未运行共享单元测试。

## 统一多层预览（2026-09-14）

从参考图提取「名称与基础信息 → 属性／效用 → 特殊能力 → 限制 → 来历 → 操作」的阅读顺序，保留项目纸墨配色，不移植参考图的底色与装饰。

所有文字统一 `text-sm`、行高 24px，图标不属于文字层级。物品名沿用品质色，分组标题用赭色，器蕴／器诀／传承名用青色，附灵属性用绿色，代价与限制用朱色，辅助标签与来历用次要墨色；颜色之外保留文字标签、分组和留白。基础信息横向换行，属性采用对齐的标签／数值列；数量、独立数值使用 `font-mono`。不为灵印增加专属底色条或放大技能名。

| 当前道具 | 内容分组 |
| --- | --- |
| 道装 | 御使境界、器胚、附灵、器蕴、器诀；保留折叠比较 |
| 图纸 | 部位、铸造境界、用途、使用条件 |
| 材料 | 品质、五行、材料特性 |
| 消耗品 | 丹药药效／灵果效果／符箓效用、代价规则；炼制和来源信息折叠 |
| 传承灵印 | 所载传承名称与具体效果，底部灵印来历 |
| 归元灵露 | 适用物种境界、洗炼效果 |
| 功法玉简 | 传承境界、所载功法 |
| 灵种 | 品质、培育用途、灵种说明 |

灵印不再重复种类与品级，也不在预览解释随机覆盖、不可撤销、同系生效规则。其他物品已有药效、成本和比较数据保留；展示适配不修改领域算法。详情弹窗不再重复显示壳标题与道具标题。历史 artifact／skill／gongfa 产品记录仍由旧产品展示兼容，不属于现行八类库存定义。

### 本轮验证

- `bun run lint`、`bun run build`（含前后端类型检查）、`git diff --check` 通过；构建保留现有的大 chunk 提示。
- 本地道友1现有物资：检查灵印、灵露、道装、图纸、材料、丹药、符箓、灵种、灵果的真实预览；未发放、消耗或移动物资。390px 下页面 scrollWidth 为 390，灵露预览正文为 286px，文字字号为 14px，操作可见。
- 功法玉简适配已按现行定义检查，但账号无样本，未完成实页验收；装备比较、商店与旧产品详情未做完整交互复验。
- 聊天入口已共用新组件，但世界频道现有历史道装快照缺少 numericVersion=2 与 baseQuality，原有 itemPresentation 的装备解析会抛错，阻断聊天快照实页验收。本轮未修改历史快照协议或数据。
- 本轮仅修改展示层，未新增或运行共享领域单元测试；旧 artifact／skill／gongfa 产品兼容展示未迁入现行库存模型。

## 预览层级收敛（2026-09-15）

现行八类库存共用 `ItemPreview`，内容契约明确为头部 `identity`、中部 `sections`、末尾 `flavor`。头部图标旁固定显示完整名称与类型（含适用品质），功能和真实使用要求按类型提供，数量为辅助行。没有要求的数据不编造等级限制。

分组标题用赭色、内容统一缩进；属性标签与数值对齐，附灵与核心药效用绿色、限制用朱色。`PreviewRow.children` 表示能力名下的效果说明，比所属分组再缩进一级。名称为 16px 半粗，正文为 14px / 24px，数值沿用 `font-mono`。简单道具省略空分组，不增加空白框或重复分隔线。

材料、符箓、灵种的描述归到底部；道装当前没有描述事实，因此不填充占位文案。丹药保留炼制信息折叠和服用限制。灵种从 `SeedFactsSchema` 的 `seedSpec.plant` 读取品质、描述、播种境界与培育线索，修正旧 `rank/description` 字段读取。浮层定位、业务操作、历史产品兼容入口保持原职责。

本轮使用本地道友1现有物品检查道装、灵印、丹药、符箓、材料、图纸和归元灵露的预览内容；360px 下灵露浮层宽 320px，左侧 8px，内部 scrollWidth 与 clientWidth 均为 318px，无横向溢出。检查 Esc 关闭及浏览器错误日志，无记录到运行时错误。没有发放、消耗或移动物资。当前随身物品缺少功法玉简、灵种、灵果样本，这三类完成代码核对，未完成真实页面验收；商店、聊天快照和装备比较未完整复验。未修改共享引擎规则，未新增或运行单元测试。

坊市货架和旧材料详情的灵种入口同步通过 `seedFactsOf` 转换完整快照，避免只在储物袋内修正。验证命令：`bun run lint`、`bun run build`（含前后端类型检查）、触及 TSX/TS 文件 Prettier 检查、`git diff --check`；构建仍有现存的大 chunk 提示。

### 灵种响应修复与 dev 接口验收

根因是 `readInventory` 在已解析的灵种事实上覆盖了旧 `rank/description` 摘要，丢弃 `seedSpec`。现已移除覆盖，背包和储藏室都返回完整种子快照。

本地道友1实页复验两种灵种、功法玉简和灵果，品质、要求、线索与末尾描述正常，浏览器无错误日志。360px 灵种浮层左侧 8px、宽 320px，内容 clientWidth/scrollWidth 均为 318px。新 dev 清空接口实测删除 29 格、解除 1 件临时穿戴道装，页面自动更新为 0/40；重复清空返回 0，非法 ID 400、不存在角色 409。通过已有发放 API 恢复相同 29 格／156 件，物品事实与格位一致、53 条储藏室记录及修订号保持不变、恢复原无穿戴状态。

`bun run lint`、`bun run build`（含类型检查）、Prettier 与 `git diff --check` 通过。Hono CLI 尝试因包未提供可执行入口失败，改用路由 `request()` 验证 staging 和 production 环境均返回 404；没有连接或写入预发布／生产服务。未新增或运行单元测试；未实测战斗占用与并发锁冲突。

### 装备预览验收调整（2026-09-15）

装备要求仅显示境界，隐藏持有数量，保留已穿戴状态；附灵不显示双加合计。器蕴／器诀以名称为默认收起的独立展开入口，展开后显示效果，器诀只显示直接战意消耗。物品浮层只响应自身 toggle，避免内部详情收起时关闭整个预览。储物袋移除所有移动／合并入口、取消移动按钮与目标格选择状态，保留转存、拆分和整理。

本地道友1现有青石剑实页核对默认收起、分别展开／收起及消耗 90；普通材料仍显示持有数量、转存和拆分，没有移动入口。360px 下装备浮层宽 320px、左侧 8px，内部 scrollWidth/clientWidth 均为 318px。未穿脱、移动或消耗道具。本轮执行 lint、完整 build、git diff --check；未运行单元测试（只修改前端展示与交互）。

### 数值文案约定

玩家可见的数值说明使用阿拉伯数字与对应符号，比例统一写作 `1%`、`5%`，不使用“1个百分点”“五个百分点”等混合表达。此约定只改变展示，保留原有加算／乘算规则。装备器蕴、灵兽技能、宗门效果和炼器提示已同步；lint、完整 build、装备器蕴现有测试（11 项）与 diff 检查通过。本轮为文案替换，未重复浏览器验收或运行全量单元测试。

### 图纸预览精简

图纸名称统一为“境界＋期＋部位”，如“炼气期法兵”；类型仍显示“道装图纸”。头部显示“产物境界：炼气期”，表示成品档位，不展示炼制门槛。移除重复的部位／铸造境界资料分组，保留数量及完整描述，如“记载法兵铸造之法的图纸，铸造时消耗1张。”。沿用公共预览的空分组省略规则。

### 传承灵印预览精简

传承灵印的共享道具名称直接采用所载技能名称，如“噬血”“高级连击”，类型仍为“传承灵印”。“所载传承”分组直接展示技能说明，不重复技能名称，也不增加第三级缩进。底部灵印描述及品质色保持原有规则，物品标识和堆叠规则不变。

### 归元灵露预览精简

灵露不重复显示类型，保留“功能：灵兽洗炼”。普通灵露要求简写为“不高于金丹”，上品为“不限境界”。“洗炼效果”集中说明等级重置、资质／成长／天生技能重孕及寿命恢复；普通灵露底部描述保留“涤去后天积累。”，上品只保留高阶物种适用及不额外提高洗炼概率的说明，避免重复效果和境界枚举。

### 功法玉简预览精简

共享道具名称直接使用功法本名，如“轻风诀”，类型仍为“功法玉简”。传承境界放在头部类型下方，不再单独生成道具资料分组。“所载功法”下直接展示功法效果描述，移除重复功法名和第三级缩进；底部玉简描述保留。

### 丹药预览精简

丹药移除丹评、头部要求和道具资料分组，丹相移到头部功能上方，形成“类型 → 丹相（有值时）→ 功能 → 数量”的顺序。核心药效、代价与规则、炼制信息及描述保持原有内容。灵果的精简规则见下节。


### 灵果预览精简

灵果移除头部“要求”和通用“造化来源”分组，不再显示无角色境界时的“进入背包后判断”或符合条件时的提示。保留类型、功能、数量、实际效果及原始描述，不展示无丹毒、不占服用额度等默认信息，仅保留药力过强／过弱的具体境界限制及有角色状态时的服用收益预览。

## 展示职责重构验证（2026-09-15）

- 八类适配器覆盖由 TypeScript 静态注册表校验；公共组装与渲染层没有道具 kind／definitionId 特判，也不再根据文案内容删除字段或分组。
- 本地 `127.0.0.1:5174`、本地道友1：检查青石剑、炼气期法兵、噬血、归元灵露、轻风诀、自测玄铁、赤纹果核、青元养脉丹、青露灵果和空白传音符的真实预览。装备保留独立器蕴／器诀展开及直接消耗 90，丹药炼制信息展开／收起正常，已删除的字段未恢复。
- 360px 下灵露浮层宽 320px、左侧 8px，内部 scrollWidth 与 clientWidth 同为 318px；错误日志无运行时错误。测试未发放、消耗、穿脱或移动道具，结束后恢复视口。
- `bun run lint`、`bun run build`（含前后端类型检查）、触及文件 Prettier、`git diff --check` 通过；构建保留既有大 chunk 提示。
- 未新增或运行单元测试：本轮不改共享引擎规则，前端依项目规范使用静态检查与浏览器验证。角色未穿戴同槽对比样本，装备比较完成代码核对，未做实页对比；拍卖、聊天快照及符箓实际使用确认未完整复验。
