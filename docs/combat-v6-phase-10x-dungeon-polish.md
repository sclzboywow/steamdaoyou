# Phase 10X：秘境材料消耗统一与战前确认恢复

> 2026-09-11 后续变更：[10Y](combat-v6-phase-10y-dungeon-recovery.md)按用户补充将本篇自动选材规则替换为公共物品栏自主提交，并补异常结果恢复。本篇保留10X实施时的历史决定与证据。

2026-09-11 用户确认实施；状态：实现与重点本地验收完成，剩余矩阵待验。候选为 `codex/combat-v6` / `88493635` 上本阶段工作区变更，尚未发布。来源：[收尾规划G01](combat-v6-polish-and-closure-plan.md)。

## 确认边界

- 探索选项材料仅使用新版bag。历史材料须先从宝库取出，storage须先取回；秘境期间沿用物品占用规则，不增加途中存取。
- 保留自动选材：满足名称、类型、最低品质，优先最低合格品质，同品质按格位稳定选择；可跨堆叠凑足数量，多个要求共同核对，不重复使用同一份材料。
- 材料与其他成本、探索状态和费用记录同事务提交；预检与实际消费共用匹配规则。复用actionId、runId和轮次防止重复或陈旧操作支付下一轮。
- 选择战斗选项后先进入WAITING_BATTLE，固定敌方与入场输入，只下发公开遭遇摘要；点击迎战才创建可操作战局。刷新或离页不重抽、不自动开战、不重复扣费。
- 确认页展示遭遇描述、守敌名称／数量、本人HP/MP和出战灵兽，提供迎战与结束探索。结束探索沿用提前离开结算，只领取此前已确定收益，无本场或通关奖励。
- 确认期间继续占用秘境，不开放换装、服药、选宠或其他战斗；既有恢复丹药仅在探索／战后休整时可用。
- 不新增表、通用库存框架、手动选材UI，不修改难度、掉落、收益、战斗药品或其他玩法。

## 实施与验收

沿用当前状态持久容器和战前scene descriptor，旧神识查探／敌方精确面板不恢复。

### 已实现

- `src/shared/lib/dungeon/materialCosts.ts` 负责纯材料匹配与扣量规划；固定材料与历史取出的转换材料共用事实读取。服务端 `costs.ts` 只查询bag，通过既有库存revision校验保存，与资源、状态和费用账本一起提交。
- `prepareDungeonEncounter` 固定人物、携带灵兽与敌方输入，保存在既有run的私有battlePayload中。公开state只包含确认所需摘要；`POST /api/dungeon/battle/begin` 才创建战局，同一遭遇重复迎战返回同一battleId。
- 探索请求携带runId、round和稳定actionId；已提交行动重放返回当前状态，陈旧轮次的新操作及不允许行动的状态拒绝执行。
- 新增战前正文并复用秘境壳层。实测修复了退出结算后旧WAITING_BATTLE分支遮挡结算页的问题。

### 本地真实链路（2026-09-11）

环境为本地Vite 5174、API 3001及本地PG／Redis；使用既有本地道友2。为地图门槛临时将炼气后期改为筑基初期，未提升六维；结束后已恢复原境界，保留真实消耗与所得。

- run `3a6d81cf-b997-4cd3-bd6a-787d847d7e74`：首场选择后为WAITING_BATTLE，activeBattleId为空，费用记录1条。刷新、返回洞府再进入后遭遇ID与私有输入哈希保持一致；桌面及360×800确认页无横向溢出，迎战／结束探索均可见。
- 明确迎战后产生battle `18bfb872-84e9-4ebe-8da1-71d40f09672c`；重复迎战HTTP 200且仍为原战局／单条费用，无效遭遇ID为409。通过自动战斗正常胜利、完成战斗并继续探索。
- 第3轮实际支付凡品aux一份：`material.aux.qingxi-stone.v1` 从2变1，revision更新。重放该行动HTTP 200后仍为1；旧第1轮的新行动HTTP 409。费用账本未重复，正常推进到第4轮。
- 第4轮确认显示古修士残魂、秘境精锐两名守敌；气血1521/1654、法力876/956、疾风狼与上一场结束一致，未创建第二场战局。
- 从该确认页结束探索，页面进入结算；run为FINISHED且无activeBattleId。实际到账灵石150、修为325、青溪兽骨1，与5条已确定收益一致；无第二场战斗或通关奖励。辅料仍剩1份，人物战斗消耗保留。

### 检查与剩余范围

- `bun run test src/shared/lib/dungeon src/shared/engine/combat-v6/dungeon src/shared/rewards/dungeon.test.ts`：4文件24项通过。其中5项新增纯逻辑用例覆盖跨堆叠／品质顺序、指定名称保留、重叠不足不改输入、storage排除、固定与转换材料。
- `bunx tsc -b tsconfig.app.json tsconfig.node.json --pretty false`、`bun run lint`、`bun run build`、`git diff --check`通过；退出页面修复后重新运行lint／build。未重复全仓共享测试，未新增前后端或数据库单测、一次性验收脚本；未执行迁移或发布。
- 真实浏览器未覆盖仅旧宝库有物／storage有物、跨堆叠混合品质、材料不足重选、支付事务故障／并发竞争，以及战前全部构筑入口互斥。匹配规则已有共享测试与事务代码检查，但不能替代这些运行验收；G01相应子项继续待验，G11其余秘境矩阵保持原状态。
- 本地首次场景生成被既有LLM结构校验拒绝，重试正常成功，未放宽校验；继续探索曾出现一次空响应JSON解析提示，服务端完成HTTP 200后刷新恢复到下一轮，原因未定位。该恢复体验留作后续排查，不声称本次修复。
