# Phase 10V：旧秘籍抽取退役与万界商行新版入库

2026-09-10 用户确认旧抽取先下线，已有专属符箓和秘籍暂不转换、不补偿，后续独立设计抽取玩法。主体实施与重点本地验收完成。

## 边界与实现

- 删除旧抽取页面、导航、HTTP 路由、两层服务、专属 DTO、概率配置及其测试。不提供隐藏入口或旧接口兼容。
- 保留历史符箓、秘籍、道具库及邮件数据；历史符箓展示停用说明，移除跳转。没有删除或转换玩家资产。
- 道具库发布与新奖励生产禁止旧抽取专属符箓／秘籍；历史邮件领取仍走原历史通道，不在库存读取或历史领取入口加入禁用校验。后台符箓场景选项移除旧抽取。
- 万界商行仅接受可生成新版库存事实的材料、种子、丹药／灵果及有效场景符箓。玩家列表隐藏不支持的存量商品，直接购买与后台上架同样拒绝。后台仍可查看、归档旧商品。
- 复用现有新奖励事实构建和 grantInventory，优先 bag，满包 storage。声望扣除、库存写入和周购买记录同一玩家事务；ResourceEngine 只处理声望，不整体改造历史发物能力。
- 请求必须携带 UUID requestId；复用玩家命令持久凭据，以商品 ID 为指纹。同键重试返回原结果，不再扣费或发奖；同键换商品返回 409。
- 页面保留原兑换结构，增加同步提交保护与按商品保留的失败重试凭据；成功后刷新声望、额度并提示入库位置。新版库存页进入时读取当前事实，不新增平行库存订阅协议。
- 保留价格、每份数量、周限购与历史附件规则。不增加道装／玉简／兽诀商品类型，不重新设计抽取、闭关或高阶产出，不执行迁移、补偿、删表、部署。

## 本地验证

使用现有 local1 会话，Web 127.0.0.1:5174、API 3001；核对配置指向 127.0.0.1/daoyou_local。

| 场景 | 结果 |
| --- | --- |
| 旧抽取页面 | 显示既有 404 页面 |
| 旧 POST /api/manual-draw 与 GET /status | 404，接口不存在 |
| 万界商行真实页面兑换 | 声望 55→54，周剩余 2→1，满包种子进入 storage |
| 临时腾一格后兑换 | 声望 54→53，种子进入 bag，周剩余归零 |
| 同 requestId 重放 | 200，replayed=true，声望仍53 |
| 新请求超过周限购 | 400，未继续扣费 |
| 同键换商品／缺 requestId | 分别409／400 |
| 旧悟道演法符上架 | 400，对应玩法已停用；历史道具库查询仍200 |
| 本地新奖励邮件发旧符箓 | 被拒绝；dev入口将领域错误包装为500，没有成功发放响应 |
| 响应式 | 常规视口完成真实兑换；360px空列表DOM宽度360，截图合成存在裁切现象，不作为完整移动视觉验收；视口已恢复 |

通过 lint、TypeScript、两端 build、131文件／876项 shared 测试。删除旧概率测试3项，新增纯停用边界测试2项。构建保留既有大 chunk 提示。本轮浏览器日志工具未返回 error/warn；不据此宣称所有历史控制台问题已排除。

本地影响：临时商品 dcde1e97-8404-4214-ba80-090cae176477 已归档；两次真实兑换记录保留，声望净减2，储藏室新增寒渊冰魄种2枚。临时转存的130级法衣图纸已恢复bag，数量2不变、revision推进至10。没有清空物品、修改其他角色进度或生产数据。

## 上线维护与后续核对

- 后续用户确认保留旧秘籍材料类型、现有生成能力与权重，供未来用途设计；不执行旧秘籍材料的全局退役或道具库批量下架。旧抽取仍停用，已有符箓与秘籍不转换、不补偿。
- 维护期归档不支持的万界商行商品、停止旧抽取专属符箓的新生产。材料类型保留不等同于开放新版库存、兑换或恢复旧功法／神通用途。
- 逐一核对新手、任务、兑换码、广播等新奖励配置，标识会被10V当前发放校验拒绝的条目。保留旧秘籍生成的决定不自动解除10V商品与新奖励限制；发现有效业务仍依赖该发放时须单独明确用途与去向，不静默换物或丢弃附件。
- 历史待领邮件、玩家已持有物品不在该清理范围，不批量改写。
- 通用材料生成器及材料库补货的旧秘籍类型按后续决定保留。秘境正式奖励已经使用V6确定性掉落；遗留类型名称不能作为其仍发放旧秘籍的证据。
- 历史邮件实际领取、普通材料／丹药／有效符箓逐类兑换、声望不足、跨周、断网与多进程故障矩阵尚未本轮逐项运行。前序验收余项继续保留。

全部文件编辑使用内置 apply_patch；保留原有 eslint.config.js 改动。收尾时发现工作区另有17份 combat-v6 测试文件被删除，非本轮操作，未恢复或纳入本阶段；上述131文件／876项测试记录对应这些外部删除出现前，不能据此声称最终工作区仍有相同测试数量。最终 lint、类型检查和 diff 检查通过。未提交 Git、未执行生产维护或数据库迁移。
