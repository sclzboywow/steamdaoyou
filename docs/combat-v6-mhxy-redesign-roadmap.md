# combat-v6 梦幻式战斗体系设计稿与迁移路线图

> **2026-09-11 通用AUTO：**用户跳过10AA，确认[Phase 10AB](./combat-v6-phase-10ab-auto-utility.md)。主动AUTO、天骄榜等非实时对战与所有NPC使用效用评分及内部策略权重；评分仅使用允许观察的信息。手动／超时补令保持现状，不新增蜃楼数据库持久化。验收结果见阶段记录。

> **2026-09-11 秘境连续战斗收尾：**[Phase 10Z](./combat-v6-phase-10z-dungeon-continuity.md)修正休整列表旧宝库来源与底层扣药占用冲突，复用公共物品栏按具体物品版本服药。真实连战法力继承、换宠经验与战败刷新结算已验；剩余组合及发布矩阵保留待验。

> **2026-09-11 秘境收尾更新：**[Phase 10Y：自主选物与异常恢复](./combat-v6-phase-10y-dungeon-recovery.md)已实现并完成重点本地验收。用户补充将10X自动选材替换为公共物品栏自主提交；异常结果先读取核实，结算可按原runId恢复。剩余运行矩阵见专项记录，不等同发布放行。

> **2026-09-11 内容治理规划：**后续各类 build 的策划数据包与代码职责治理见[构筑内容配置化与代码治理规划](./combat-v6-build-content-governance-plan.md)。已完成领域盘点，首轮道装基础与阵纹范围待讨论确认；不替代现有发布验收，不代表各轮已经实施。

> **2026-09-11 收尾进展：**发布仍处于10W验收收尾；境界功法与领域归属重构之后，已实施[Phase 10X：秘境材料消耗统一与战前确认恢复](./combat-v6-phase-10x-dungeon-polish.md)，完成重点本地验收，剩余矩阵见专项记录。跨阶段遗留、简化设计、明确延期和已替代事项统一见[V6打磨收尾规划](./combat-v6-polish-and-closure-plan.md)；本篇各Phase保留历史记录，不将主体实现视为生产放行。

> **2026-09-10 功法方案更新：**本篇／真解、六道印与覆盖散功规则已由[境界功法层数养成](./combat-v6-manual-system-design.md)替代。炼气至元婴逐境界开放四位，每位多本独立培养、同时一本生效、免费切换；修为＋感悟升层、同名玉简破瓶颈。首批十六本固定六维单／双效果功法采用 JSON 数据包。下文 5A／9F 及旧决策表为历史阶段记录，冲突处以新设计为准，不做旧数据兼容。

> 状态：方向基线（Canonical）  
> 建立日期：2026-09-01  
> 适用分支：`codex/combat-v6`  
> 建立时基线提交：`f9510ab5`  
> 核心目录：`src/shared/engine/combat-v6`  
> 角色面板：[`combat-v6 角色六维与战斗面板设计`](./combat-v6-character-panel-design.md) 修炼系统：[`combat-v6 新版修炼系统设计`](./combat-v6-training-system-design.md) 宗门系统：[`combat-v6 宗门心法、技能与经脉系统设计`](./combat-v6-sect-skill-meridian-system-design.md) 红尘剑宗纵切：[`combat-v6 红尘剑宗纵切设计`](./combat-v6-lingxiao-datang-sect-design.md) 九劫天宫纵切：[`combat-v6 九劫天宫经典双流派纵切`](./combat-v6-jiujie-sect-design.md) 召唤兽系统：[`combat-v6 召唤兽系统设计`](./combat-v6-summoned-beast-system-design.md) 兼容性明细：`src/shared/engine/combat-v6/CHARACTER_COMPATIBILITY.md`

本文档是 combat-v6 后续设计和实现的方向基线。代码实现、数据模型、内容配置和迁移策略若与本文冲突，应先修改本文并记录决策，再修改代码，避免实现过程中重新滑回 battle-v5 / SPA + GAS 的设计方式。

本文锁定的是架构边界、系统归属、迁移原则和交付顺序；具体数值、技能系数、掉落成本仍可在对应版本的平衡表中迭代。

---

## 1. 最终目标

在 Daoyou 内建立一套与 battle-v5 完全隔离的 `combat-v6`，以梦幻西游电脑版式的 we-go 回合战斗为核心：

1. 全体单位先提交指令，锁定后按实时速度顺序串行结算。
2. 引擎只提供通用战斗原语，不按宗门、技能、装备或经脉 ID 写分支。
3. 技能、状态、宗门、经脉、功法和装备通过数据与编译器接入。
4. 战斗结果由种子、输入快照和版本号完全决定，可复盘、可对拍。
5. 角色数据库继续保存长期养成事实，不保存可重新计算的战斗面板。
6. 旧 build 系统不迁就 v6；除角色基础事实和明确指定的养成进度外，装备、功法、宗门战斗内容全部重做。

这不是给 battle-v5 增加一种战斗模式，也不是让同一份 `AbilityConfig` 同时兼容两个引擎。combat-v6 是新的战斗领域，battle-v5 只在过渡期维持旧玩法和旧记录。

---

## 2. 已确认的产品决策

### 2.1 继承什么

角色迁移到 v6 时只继承以下权威事实：

- 角色 ID、名称、性别、种族等身份信息。
- 修为境界与境界阶段。
- 六项基础属性：体魄、力道、灵力、根骨、身法、神识。
- 持久气血、法力和允许进入新战斗体系的长期状态。
- 当前炼体五轨的等级；其旧效果不继承，改造成新的修炼系统效果。
- 宗门社会关系中仍有价值的事实，例如所属宗门、职位、贡献等；宗门战斗内容不继承。

### 2.2 不继承什么

以下内容不做语义自动转换，不作为 v6 生产运行时输入：

- battle-v5 `AbilityConfig`、Buff、Listener、Effect 和 GameplayTags 投影。
- creation-v2 生成的旧技能战斗配置。
- 旧功法的战斗属性与被动。
- 旧法宝/装备的战斗属性、能力树和三槽装备规则。
- 旧宗门技能、固定四技能装配和旧经脉战斗投影。
- 旧炼体五轨产生的十类 modifier、Buff 和境界被动。

旧内容后续采用冻结、补偿、兑换或重新生成，不编写一个长期存在的“v5 内容翻译器”。

### 2.3 新体系组成

v6 build 由以下互相独立的来源组成：

- 角色基础面板投影。
- 新修炼系统（由炼体五轨改造）。
- 新装备系统。
- 新功法系统（角色版“魔兽要诀”）。
- 新宗门心法与主动技能。
- 新宗门经脉方案。
- 未来的召唤兽、阵法、药品与特技系统。

所有来源最终只能输出 v6 认识的有限产物：面板贡献、主动技能、被动技能、状态定义、技能覆盖和单位标签。

---

## 3. combat-v6 架构边界

### 3.1 依赖方向

目标依赖方向：

```text
Host / PVE / Online Battle
  ├─ combat-v6/rules-daoyou
  ├─ combat-v6/content
  └─ combat-v6/projection
       ├─ character
       ├─ training
       ├─ equipment
       ├─ manuals
       └─ sect + meridian
              ↓
        combat-v6/core
```

`combat-v6/core` 不依赖任何 Daoyou 具体内容。规则层解释公式族；内容层声明技能和状态；投影层把持久数据编译为入场阵容；Host 只负责编排、提交指令、保存快照和播放事件。

### 3.2 禁止依赖

`src/shared/engine/combat-v6/**` 的生产代码不得导入：

- `src/shared/engine/battle-v5/**`
- `src/shared/engine/creation-v2/**`
- 旧宗门的 v5 战斗投影和编译产物
- v5 `AbilityConfig`、`AttributeModifierConfig`、Buff、Listener、GameplayTags

允许存在独立的开发期对照工具，用相同角色分别运行 v5 和 v6 面板计算；该工具不得进入 v6 生产投影链路。

### 3.3 核心与内容的职责

核心负责：

- 指令提交与锁定。
- 速度排序和回合推进。
- 普攻、技能、防御、保护、逃跑、召唤等通用动作。
- 目标选择、状态生命周期、效果原语、钩子总线。
- 人物倒地、召唤兽/NPC 死亡、胜负判断。
- 确定性 RNG、事件流和可序列化快照。

核心不负责：

- 角色六维如何生成战斗面板。
- 武器如何增加伤害或法伤。
- 哪个宗门拥有什么技能。
- 哪个经脉节点修改哪个技能。
- 哪本功法属于高级版本或与谁冲突。
- 任何玩法奖励、掉落、养成消耗和数据库读写。

### 3.4 引擎扩展准则

只有出现“多个无关内容都需要、现有原语无法表达”的能力时，才扩展 core。单个宗门或单个装备的特殊机制优先通过通用效果、Hook、状态或公式族表达。

严禁出现：

```ts
if (skill.id === '某宗门技能') {
  /* 特判 */
}
if (nodeIds.includes('某经脉节点')) {
  /* 特判 */
}
```

---

## 4. 版本与确定性契约

每一场 v6 战斗及其记录必须携带：

```ts
interface CombatV6VersionStamp {
  engineVersion: 'combat-v6';
  rulesetVersion: string;
  contentVersion: string;
  projectionVersion: string;
}
```

必须共同进入可复盘输入的还有：

- `seed`
- 已编译的入场单位快照
- 技能表与状态表版本或不可变快照
- 初始阵容与资源策略
- 每回合锁定指令
- RNG state

同一输入快照、版本戳和指令序列必须得到相同事件流。新版本不得覆盖旧记录的解释语义；无法继续执行的旧记录至少要能读取其已保存事件。

---

## 5. 角色基础数据兼容层

### 5.1 最小输入边界

基础角色投影器只允许读取：

```ts
type CultivatorBaseCombatInput = Pick<
  Cultivator,
  'id' | 'name' | 'realm' | 'realm_stage' | 'attributes' | 'condition'
>;
```

装备、功法、宗门和修炼由各自的 v6 投影器单独输入，不得通过扩大这个 `Pick` 偷渡旧 build 数据。

### 5.2 正式角色面板投影

角色永久六维、有效六维、裸身面板公式、点数单位和编译顺序以 [`combat-v6 角色六维与战斗面板设计`](./combat-v6-character-panel-design.md) 为准。第一版正式投影版本为 `character_panel_v1`，不得调用 battle-v5 `AttributeSet`：

```text
physicalAtk = floor(40 + strength × 3.5)
physicalDef = floor(10 + endurance × 1.75)
magicAtk    = floor(40 + spirit × 3.5)
magicDef    = floor(10 + willpower × 1.75)
maxHp       = floor(400 + vitality × 20 + endurance × 3)
maxMp       = floor(200 + spirit × 4 + willpower × 10)
speed       = speed
hit         = floor(80 + speed)
dodge       = floor(speed)
healPower   = floor(vitality × 0.25 + willpower)
sealHit     = floor(spirit × 0.5)
sealResist  = floor(willpower × 0.5)
```

道装附灵六维在上述公式之前与永久六维合并为有效六维；其他 build 默认在公式之后输出面板贡献或被动。后续调整系数、职责、取整或贡献顺序必须发布新的 projection version，不得静默改写 `character_panel_v1`。

### 5.3 当前气血与法力

战斗场景必须显式选择资源策略：

```ts
type CombatV6ResourcePolicy = 'full' | 'persistent';
```

- `full`：以编译后的 `maxHp/maxMp` 满状态入场，用于训练、镜像、部分竞技玩法。
- `persistent`：读取 `condition.resources.hp/mp.current`，再夹到新上限内，用于连续世界战斗。

战后是否回写持久资源由玩法 Host 决定，不属于 core。

### 5.4 人物战斗等级

兼容期暂定：

```ts
level = (getRealmStageRank(realm, realmStage) + 1) * 5;
```

现有 36 个境界阶段映射为 5～180 级。该等级只作为 v6 公式、技能开放和目标规则的统一标尺，不替代修为境界，也不改变原有境界持久数据。

### 5.5 裸身属性与暂缺系统默认值

基础投影直接生成六维可派生字段；尚未接入的新系统采用显式默认值：

```text
attackCultivate       = 0
defenseCultivate      = 0
spellCultivate        = 0
resistSpellCultivate  = 0
physicalFuryRate      = 0
critRate              = 0.05
spellCritRate         = 0.05
healPower             = floor(vitality × 0.25 + willpower)
sealHit               = floor(spirit × 0.5)
sealResist            = floor(willpower × 0.5)
hit                    = floor(80 + speed)
dodge                  = floor(speed)
```

core 已统一使用 `physicalFuryRate` 表示物理狂暴概率，不能与器诀使用的战意资源混用。

技能等级规则：

```ts
skillLevels[skillId] = explicitSkillLevel ?? characterLevel;
```

使用 fallback 时必须写入投影诊断，不能无声兜底。宗门技能接入后应由所属心法提供明确等级；功法被动一般不依赖人物技能等级，确有需要时由功法定义明确给出。

### 5.6 投影结果

```ts
interface CombatV6ProjectionResult {
  unit: LineupUnit;
  skills: SkillDef[];
  statusDefs: StatusDef[];
  diagnostics: CombatV6ProjectionDiagnostic[];
  versions: CombatV6VersionStamp;
}
```

投影诊断至少区分 `info`、`warning`、`error`。缺少必需映射、未知装备词条、无等级技能和冲突功法必须被报告；`error` 阻止开战。

---

## 6. 新修炼系统：由炼体五轨原位改造

完整升级、位阶、战斗公式和迁移设计以 [`combat-v6 新版修炼系统设计`](./combat-v6-training-system-design.md) 为准。

### 6.1 迁移原则

当前炼体拥有五条持久轨道：皮肤、筋骨、脏腑、气血、元神。新修炼系统不另起一套从零养成的数据，也不继续使用旧炼体效果。

锁定原则：

1. 玩家五轨的 `level` 一对一继承，任何一轨都不合并、不取平均、不丢失。
2. `progress` 原值保留；新旧继续使用同一升级阈值，不做比例换算。
3. 旧肉身阶位、milestone 和战斗 Buff 停止提供 v6 效果。
4. 新效果只输出 v6 面板贡献或被动，不生成 v5 modifier。
5. 迁移必须幂等并记录 migration version，不能重复换算。

### 6.2 五轨新定义

为完整保留五条等级，v6 采用“四修炼 + 一根基”的人物修炼结构：

| 旧轨道（迁移来源） | 新轨道 | v6 主要输出 | 迁移 |
| --- | --- | --- | --- |
| `sinew_bone` 筋骨 | 攻法修炼 | `attackCultivate` | 等级原值继承 |
| `skin` 皮肤 | 防御修炼 | `defenseCultivate` | 等级原值继承 |
| `organs` 脏腑 | 法术修炼 | `spellCultivate` | 等级原值继承 |
| `primordial_spirit` 元神 | 抗法修炼 | `resistSpellCultivate` | 等级原值继承 |
| `qi_blood` 气血 | 生命根基 | `maxHp`、`healPower` 等有限面板贡献 | 等级原值继承 |

这样既满足 combat-v6 的四项修炼字段，又不吞掉第五条已有养成。生命根基不是第五个伤害修炼差，不参与物理/法术修炼差公式。

玩家界面和持久数据继续使用皮肤、筋骨、脏腑、气血、元神原名称与 track key；“攻法修炼”等只表示 combat-v6 内部战斗作用，不要求改名或迁移丹药方向。

映射名称和效果属于 v6 新规则；旧轨道此前提供的百分比物攻、抗暴、减伤、回蓝、濒死保护等效果全部终止，不做叠加继承。

### 6.3 等级和进度迁移

继续使用现有 `condition.tracks.bodyCultivation` 持久模型，不另建平行修炼状态：

```ts
interface BodyCultivationState {
  version: 1;
  realm: BodyCultivationRealm;
  tracks: Record<BodyCultivationTrackKey, ConditionProgressTrack>;
}
```

五轨等级和进度原值保留。升级阈值继续使用现有公式：

```text
threshold(level) = 100 + 70 × level
```

五轨只通过炼体丹、灵果等合法 `advance_track` 消耗品推进，不提供消耗修为、灵石或贡献的直接修炼入口。现有丹药方向和实例无需映射。

肉身七阶继续作为阶段位阶和单轨上限展示，但取消旧专项轨道条件、材料、丹药、概率、失败和保底。达到人物境界与五轨总等级要求后，由玩家点击无消耗、确定性提升到下一位阶。

### 6.4 新效果边界

- 四项主修直接产生同名 v6 修炼值，`修炼值 = 对应轨道等级`。
- 物法伤害修炼差按 `2% + 5` 公式结算，有效差限制在 `-20～20`。
- 封禁修炼差每级修正 2 个百分点，有效差限制在 `-10～10`。
- 生命根基每级增加 `character_panel_v1` 裸身气血 0.5%，每两级增加 1 点 `healPower`，不直接改六维。
- 修炼不来源于宗门心法；转宗、换装备、换功法不会改变四修炼等级。
- 修炼也不提供宗门主动技能等级。
- 肉身位阶本身不提供 combat-v6 战斗效果，旧七阶战斗被动全部退出。

肉身七阶单轨上限依次为 5、10、15、22、30、45、60；晋升总等级门槛继续为 12、30、55、90、140、220。

---

## 7. 新装备系统

新版装备系统使用修仙世界观名称“道装”，采用梦幻式六部位、器胚白字、附灵六维、器蕴被动和器诀主动结构。完整领域设计以 [`combat-v6 道装系统设计`](./combat-v6-equipment-system-design.md) 为准。

已确认边界：

- 六部位为 `weapon / head / armor / necklace / belt / footwear`，显示为法兵、法冠、法衣、灵佩、腰封、云履。
- 道装从实例、生成、装配到战斗投影均为 v6 独立领域。
- 道装不存在品质、稀有度、颜色档次或系统权威评分。
- 玩家依据实际白字、附灵、器蕴、器诀和阵法灵纹自行评估价值。
- 道装不使用境界衰减，以器阶和御使等级自然换代。
- 附灵六维只在战斗投影期生效，不写回角色持久六维。
- 道装不提供四项修炼等级，不保存运行时 `SkillDef` 或最终角色面板。
- 淬炼永久删除；道装固定为已鉴定，不预留淬炼字段或鉴器流程。
- Phase 4A 实现器胚、附灵、阵法灵纹和完整属性投影；Phase 4B 已实现器蕴、器诀与单场战意；持久化及经济外围进入 Phase 4C。
- “法宝”保留给未来独立系统，不作为六部位装备的名称。

---

## 8. 新功法系统：角色版“魔兽要诀”

功法是跨宗门的有限槽位被动 build，通过参悟功法玉简，将被动铭刻为角色道印。完整领域设计以 [`combat-v6 功法系统设计`](./combat-v6-manual-system-design.md) 为准。

已确认边界：

- 炼气初始 2 个道印位，每提升一个大境界增加 1 个，化神达到 6 个后封顶。
- 首版每个角色只有一套功法构筑，不能保存或切换方案。
- 构筑使用独立值对象设计，保留后期迁移到多构筑方案的技术可行性，但首版不预埋切换接口和 UI。
- 首版没有固印、锁槽和对应材料。
- 槽满后由玩家明确选择改修目标，不做随机覆盖；参悟必定成功。
- 使用本篇/真解同源覆盖，不设计功法等级与经验。
- 功法只编译为 v6 被动、innate、少量白名单面板贡献和通用标签。
- 功法不授予主动技能，不修改宗门技能，不提供四项修炼。
- 首版功法名称、描述、效果、数值、掉落和构筑操作均无 AI 参与。

---

## 9. 新宗门技能系统

完整领域设计以 [`combat-v6 宗门心法、技能与经脉系统设计`](./combat-v6-sect-skill-meridian-system-design.md) 为准。

### 9.1 社会状态与战斗进度拆分

概念上拆为：

```ts
interface SectMembershipState {
  sectId: string;
  rank: string;
  contribution: number;
  // 组织、任务、设施等社会玩法事实
}

interface SectCombatProgressV6 {
  version: 1;
  sectId: string;
  methods: Record<string, number>;
  meridianDepth: number;
  activePathId: string;
  meridianLoadouts: [MeridianLoadoutV6, MeridianLoadoutV6];
}
```

可以暂时共用持久容器，但编译器和领域类型必须区分社会玩法与战斗内容。

### 9.2 六本心法

每个宗门保留六本心法，目标上限 180：

- 心法上限为 `min(180, 人物等级 + 10)`。
- 分支心法不得超过主心法等级。
- 心法决定技能开放条件。
- 宗门技能等级等于其所属心法等级。
- 心法可以提供少量明确的基础面板贡献，但不映射四项人物修炼。
- 取消 v5 `growthProfile` 通用效果倍率，技能通过所属心法等级显式成长。
- 修炼、心法和人物战斗等级是三个独立维度。

### 9.3 技能使用

- 取消“固定装配四个主动技能”的战斗限制。
- 已解锁且满足条件的宗门技能都可在战斗中使用。
- 快捷栏只是一种 UI 偏好，不是战斗能力真相。
- 所有人使用统一普通攻击；宗门可以通过投影结果中的被动或覆盖改变其行为。
- 宗门特色机制必须重建为 v6 状态、资源、被动、反应表和通用效果。
- 特殊战斗资源按机制需要配置，可为空，首版每宗门最多一种。

### 9.4 宗门投影契约

```ts
interface SectCombatProjectionV6 {
  skills: SkillDef[];
  passives: SkillDef[];
  statusDefs: StatusDef[];
  skillLevels: Record<string, number>;
  skillOverrides: SkillDef[];
  resources: CombatResourceDef[];
  panel: CombatV6PanelContribution[];
  unitTags: string[];
  diagnostics: CombatV6ProjectionDiagnostic[];
}
```

宗门内容必须先编译为该结构，再与角色、装备、功法和修炼结果组合。宗门编译器不直接创建战斗 Session。

---

## 10. 新经脉系统

经脉的完整结构、特色机制边界和旧进度迁移以 [`combat-v6 宗门心法、技能与经脉系统设计`](./combat-v6-sect-skill-meridian-system-design.md) 为准。

### 10.1 结构

每个宗门固定两个流派，每个流派采用七层经脉：

- 每层恰好 3 个互斥节点。
- 每个流派保存且只保存 1 套方案，因此每宗门共 2 套。
- 激活流派时自动启用对应方案。
- 节点选择必须满足前置层和心法/人物等级条件。
- 战斗外免费切换流派、重置和修改节点；开战后使用已编译快照。

### 10.2 节点允许输出

经脉节点只能：

- patch 已有技能。
- grant / revoke 技能。
- 授予被动。
- 增加白名单面板贡献。
- 增加单位标签。

不得在 core 按节点 ID 分支，也不得让节点直接修改 BattleSession 内部状态。

### 10.3 编译顺序

```text
宗门基础技能
→ 心法等级与技能解锁
→ 流派基础 patch
→ 经脉节点按层级顺序编译
→ 技能/被动/面板契约校验
→ 最终 SkillDef、passives、skillLevels、skillOverrides
```

同一技能发生多个 patch 时必须有稳定顺序和冲突检测。不可组合的节点在战斗前报错，不允许后写覆盖掩盖内容错误。

---

## 11. 最终入场面板编译

### 11.1 编译管线

```text
角色身份 + 永久六维 + 境界
→ 道装附灵六维合成有效六维
→ character_panel_v1 裸身面板
→ 新修炼贡献
→ 道装器胚与阵法灵纹固定贡献
→ 新功法贡献
→ 宗门心法贡献
→ 经脉 patch 与贡献
→ 长期状态修正
→ 资源策略
→ 校验、诊断、封装 LineupUnit
```

所有贡献先在投影层归并，再把最终 `Attrs` 交给 core。core 不知道属性来自哪里。

### 11.2 面板贡献协议

第一版应采用小而明确的协议，例如：

```ts
interface CombatV6PanelContribution {
  sourceType:
    'training' | 'equipment' | 'manual' | 'sect' | 'meridian' | 'condition';
  sourceId: string;
  attr: AttrName;
  mode: 'flat' | 'percentBase';
  value: number;
  priority?: number;
}
```

不要照搬 v5 的 `BASE/FIXED/ADD/MULTIPLY/FINAL/OVERRIDE` 六阶段体系。若未来确有多乘区需求，应基于 v6 实际公式建立更小的、可审计的贡献规则。

### 11.3 校验

入场前至少校验：

- 必需面板值存在且为有限数。
- `hp/mp` 不超过新上限且不为非法负值。
- 技能 ID 唯一，所有引用均可解析。
- 每个主动技能都有明确等级。
- 被动冲突已解决。
- 经脉 patch 目标存在。
- 装备属性在白名单内。
- 版本戳完整。

---

## 12. 与现有机制的处理原则

| 现有机制 | v6 方向 |
| --- | --- |
| 六维基础属性 | 保留为持久真相，按 `character_panel_v1` 公式投影 |
| 当前 HP/MP | 按场景资源策略读取 |
| 境界 | 保留；兼容期映射 5～180 人物等级 |
| 种族 | 首版不进入 combat-v6 投影，不生成标签、面板、技能或被动 |
| 灵根 | 首版不进入 combat-v6 投影，不生成元素标签、共鸣、失配、技能或被动 |
| 命格 | 首版不进入 combat-v6 投影；战斗外经济、修炼或叙事作用不受影响 |
| 境界压制 | 默认不迁移；若保留，进入 rules-daoyou 且版本化 |
| 物法穿透 | 默认退出通用面板；个别技能可用公式/被动表达 |
| 暴击抵抗/暴伤减免 | 不直接迁移；有明确内容需求再增加通用机制 |
| 旧炼体 modifier/被动 | 全部停止，只有五轨等级迁移到新修炼 |
| 旧装备/法宝 | 不语义迁移，冻结并补偿/兑换 |
| 旧功法 | 不语义迁移，重建功法书系统 |
| 旧宗门战斗内容 | 全部重写为 v6 内容 |
| 宗门社会玩法 | 可保留并与新战斗进度解耦 |
| 旧战斗记录 | 继续由 v5 读取，不强转 v6 |

---

## 13. 迁移实施路线图

每一阶段都必须满足完成条件后再扩展下一阶段，避免同时重做所有内容导致接口失控。

### Phase 0：方向冻结与基线保护

目标：确立本文档和 v6 隔离边界。

交付：

- 本设计稿成为 canonical 文档。
- combat-v6 README 指向本文。
- 记录当前复制基线和来源。
- 增加架构测试，阻止 v6 导入 battle-v5 / creation-v2。

完成条件：

- v6 core 独立测试通过。
- 依赖扫描能阻止违规导入。

### Phase 1：Daoyou Ruleset 与基础角色投影

目标：只用角色基础事实启动一场 v6 人物战斗。

交付：

- `rules-daoyou` 第一版。
- `character_panel_v1` 面板编译器。
- 5～180 人物等级映射。
- `full/persistent` 资源策略。
- 投影诊断和版本戳。
- 裸角色 1v1 黄金测试。

完成条件：

- 不加载旧装备、功法、宗门也能稳定开战。
- 相同 seed 和快照得到完全相同事件流。

### Phase 2：炼体五轨迁移为新修炼

> 状态：已完成（2026-09-02）

目标：玩家继承五轨等级，v6 获得四修炼和生命根基。

交付：

- 继续使用 `BodyCultivationState`，迁移只停止旧 breakthrough/milestone 运行时语义。
- 五轨等级、进度、名称、key 和丹药方向原值保留。
- 四修炼 + 生命根基的新效果编译。
- 旧炼体效果与 v6 彻底断开。
- 确定性肉身位阶提升、单轨上限和新版效果预览。

完成条件：

- 任意旧角色迁移前后五条等级与进度完全一致。
- v6 只读取新修炼投影，不读取 `bodyCultivation` 的旧战斗效果。
- 不存在直接修炼入口；现有炼体丹和灵果继续生效。
- 重复执行迁移不会改变等级、进度或重复提升位阶。

实际实现补充：采用惰性兼容，不执行数据库批量迁移；旧 `milestones` 原样保留但不生效，成功晋升时清除旧 `breakthrough`。`character_training_v1` 负责五轨组合投影，battle-v5 旧炼体 modifier、开战 Buff 与位阶 Hook 已删除。

### Phase 3：一个完整宗门纵切

> 状态：已完成（2026-09-03）
>
> 实际纵切：[`红尘剑宗——斩尘证道／万剑归一`](./combat-v6-lingxiao-datang-sect-design.md)

目标：先把一套内容 DSL 做稳定，再扩展所有宗门。

交付：

- 一个宗门的六本心法（0～180）。
- 全部基础主动/被动技能。
- 至少一个完整流派。
- 七层经脉，每层恰好三个互斥节点。
- 技能 patch、grant/revoke、面板和被动编译验证。

完成条件：

- 宗门内容中没有 core 特判。
- 所有技能等级都来自所属心法。
- 经脉不同方案可对拍并产生稳定差异。

实际实现补充：红尘剑宗以 `lingxiao` 为社会身份 ID，交付六心法、六个基础技能、四个流派/经脉技能、两条完整流派和42个可观察节点。`character_sect_v1` 只读取显式 `SectCombatProgressV6`，不读取旧 `CultivatorSectState`。剑意、动态目标数、条件忽防、击倒归因和资源事件均由通用 core 原语表达，内容层不存在 battle-v5 或 creation-v2 依赖。

### Phase 4A：道装属性纵切

> 状态：已完成（2026-09-03）

目标：按独立道装设计建立六部位装备、确定性生成与完整属性投影。

交付：

- 道装实例、六槽装配和器胚属性白名单。
- 器阶、器胚、附灵及确定性生成器。
- 九种阵法灵纹注册表、部位/等级校验和固定面板贡献。
- `character_equipment_v1` 完整投影和纯逻辑装配比较。
- 无品质、无评分、无淬炼的领域契约。

完成条件：

- 装卸道装只改变 v6 投影，不改变角色持久六维真相。
- 道装实例不存在品质、稀有度、评分或运行时战斗对象。
- 道装领域不依赖其他装备模型、生成器和投影链路。

实际实现补充：`dao_equipment_generator_v1` 固定生成器胚和附灵；`character_equipment_v1` 按“附灵六维→裸身面板→生命根基→固定道装→宗门比例节点→资源”的顺序投影。空槽合法，固定已鉴定；任何非空器蕴或器诀引用在本阶段都会明确阻止投影。

### Phase 4B：器蕴、器诀与战意纵切

> 状态：已完成（2026-09-03）

目标：在 4A 稳定属性事实之上，交付七种器蕴、九种器诀、单场战意、generator v2、特殊编译/投影/比较和确定性战斗纵切。

完成条件：v1 随机黄金流不变；v2 特殊内容确定生成；战意按实际气血损失结算且行动封顶；器诀消耗、目标、状态和封禁交互可对拍；`character_equipment_special_v1` 完整携带版本戳。

实际实现补充：战意为 0～150 的人物单场资源，与剑意并存；器诀等级固定 0，归元通过单位 override 折扣。Phase 4A 入口继续拒绝特殊内容，没有兼容行为漂移。

### Phase 4C：道装持久化与经济外围

目标：在 4B 纯逻辑稳定后，另行设计实例持久化、背包装配、生成来源、炼器、掉落、市场、重铸、锁灵、灵纹升级服务和 UI。

边界：不得重新引入品质、评分、淬炼、套装、耐久、负属性或多孔灵纹；不屈、纳灵、回春、慈航渡世等复杂特殊效果需另行评审，不反向扩张 4B。

### Phase 5A：新功法纯逻辑与完整人物构筑

> 状态：已完成（2026-09-03）

目标：交付角色版“魔兽要诀”被动 build。

交付：

- 境界派生槽位、纯逻辑参悟、明确覆盖和散功。
- 本篇/真解同源层级、冲突组与跨系统能力键。
- 功法到 v6 passives/innate 的编译。
- revision 并发保护与未来多构筑迁移边界。
- 无 AI 的十个首批功法谱系、本篇/真解共二十个定义。
- `projectCultivatorToCombatV6` 完整人物构筑入口。

完成条件：

- 炼气至渡劫槽位数严格为 2、3、4、5、6、6、6、6、6。
- 功法改修不影响宗门心法、人物修炼和道装状态。
- 所有冲突都在开战前被确定性解决。
- 首版状态、接口和 UI 不包含多构筑切换、固印或 AI 链路。

实际实现补充：功法伤害强化只作用主动直接伤害；饮血、回生和回合恢复不读取治疗倍率。统一能力解析支持 `stack/unique/highest`，但不强行为既有无重叠宗门和道装内容补能力键。版本发布为 `daoyou_character_build_content_v1` / `character_build_v1`。玉简实例、库存、事务、API 和 UI 留给后续产品接入阶段。

### Phase 6A：多宗门编译与幽都经典双流派纵切

> 状态：已完成（2026-09-03）

目标：把红尘专用完整构筑升级为多宗门注册表，并以幽都验证固定伤害、毒、独立伤势、倒地锢魂和法术驱动物理群攻。

交付：

- `CombatV6SectId`、`COMBAT_V6_SECT_DEFINITIONS_V1` 与跨宗门 ID 校验。
- 幽都六心法、共享基础技能、“勾魂阎罗/六道魍魉”双流派和42个经脉节点。
- `DamageKind.Fixed`、`wound/recoverableHp`、效果级目标与物理 `cannotMiss`。
- `daoyou_rules_v2`、`character_build_v2` 与 `projectCultivatorMultiSectToCombatV6`。
- Phase 1～5A 旧入口、版本戳和红尘结果继续保留。

边界：不读取旧幽都进度，不接持久化、API、页面、Host 或战后永久伤势；无相禅宗、天衍圣地和九劫天宫继续留在后续宗门阶段。

### Phase 6B：无相禅宗双流派与治疗防护纵切

> 状态：已完成（2026-09-03）

目标：以无相禅宗验证治疗、护盾、疗伤、稳定净化、复活、短期强化资源循环和纯法术输出。

交付：

- 可序列化护盾、疗伤、可配置净化和技能成功结算段。
- rules v3 法术系数、分灵和法术忽防结算。
- 无相六心法、共享六技能、“慈航渡厄/明王镇狱”双流派和42个经脉节点。
- 0～6 念资源、主动无相诀与两流派各自的强化窗口。
- 三宗门 V2 注册表、`character_build_v3` 与 `projectCultivatorMultiSectV3ToCombatV6`。
- Phase 1～6A 旧入口、版本戳和黄金结果继续保留。

边界：不读取旧无相形态或进度，不接持久化、API、页面、Host、AI 选招和战后永久伤势；天衍圣地、九劫天宫及其他宗门留待后续纵切。

### Phase 6C：天衍圣地河洛九宫五行法印与反应纵切

> 状态：已完成（2026-09-04）

目标：以天衍圣地验证无施法者使用权的协同法印、有向反应矩阵、逐目标有效冲击伤害和主动衍数决策。

交付：

- 状态精确消费/复制、目标状态要求、逐目标有效冲击伤害和通用 `MechanicTriggered` 事件。
- 天衍六心法、五门落印术、三门共享辅助、十种反应和0～3衍数。
- “河图演生/洛书制化”双流派、42节点、两门终式和两门三层主动。
- 四宗门V3注册表、`daoyou_rules_v4`、`character_build_v4` 与 `projectCultivatorMultiSectV4ToCombatV6`。
- Phase 1～6B旧入口、版本戳和黄金结果继续保留。

边界：不读取旧天衍进度，不接灵根、四技能栏、持久化、API、页面、Host、AI或战后回写。

### Phase 6D：九劫天宫天律镇妖与九霄驭雷纵切

> 状态：已完成（2026-09-04）

目标：以经典封天宫/魔天宫为原型重建九劫，验证成本后概率裁定、非致命重创、三类封禁、随机物理攻击目标和跨施法者电芒引爆。

交付：

- 通用 `RandomBranch` / `ChanceResolved`、`cannotKill`、状态层数表达式和 `RandomAttackTarget`。
- 九劫六心法、共享六技能、镇妖/错乱/百万神兵三封和五雷轰顶。
- 无归属电芒叠层、协同消费、单体/群体引爆与确定性事件归因。
- “天律镇妖/九霄驭雷”双流派、42节点和四门授予技能。
- 五宗门V4注册表、`daoyou_rules_v5`、`character_build_v5` 与 `projectCultivatorMultiSectV5ToCombatV6`。
- Phase 1～6C旧入口、版本戳和黄金结果继续保留。

边界：不读取旧九劫进度，不保留劫雷、劫债、劫数、行动后处罚或整技能免疫；不接持久化、API、页面、Host、AI、PVE Boss专属抗性、战后回写或雷属性克制。

后续宗门仍须逐个交付六心法、共享基础技能、双流派、42节点与黄金测试，不接受直接接入旧 `AbilityConfig` 的临时版本。

### Phase 7A：确定性遭遇编排与训练 Host

> 状态：已完成（2026-09-04）

目标：在共享纯逻辑层把五宗门完整人物构筑、独立训练 NPC、确定性策略和 `BattleSession` 编排为首个可逐回合驱动的 PVE 闭环。

交付：

- 60/120/180 三档显式 NPC 面板和单木桩、攻击陪练、三木桩、治疗防护、净化、复活六类遭遇。
- 始终防御、普通攻击和固定技能轮转三种确定性 NPC 策略。
- 不消费 RNG 的结构化指令查询，执行失败继续由 core 权威裁定。
- `training_encounter_v1`、纯逻辑 Encounter Host、训练结果摘要和非持久调试转录。
- 五宗门继续通过 `character_build_v5` 投影，以 `full` 策略进入训练战斗。

边界：不接数据库、API、页面、奖励、战后回写、checkpoint、持久录像、高级 AI、召唤兽或旧训练房。

专项设计见[`combat-v6 Phase 7A：确定性遭遇编排与训练 Host`](./combat-v6-phase-7a-training-host-design.md)。

### Phase 7B：权威构筑持久化与 v6 训练房

> 状态：已完成（2026-09-04）

交付：

- membership 归属的独立 combat-v6 构筑持久态，以及五宗门 active membership 的六心法单次迁移。
- `pending / uninitialized / active` 初始化流程和一次性流派选择。
- 服务端 `character_build_v5` 权威装配器，不读取 battle-v5、creation-v2 功法或旧装备。
- 每角色单会话、30分钟 TTL、乐观 revision、事件游标和 membership 变更失效的进程内训练 Store。
- 只接受遭遇、档位与五类指令的训练 API，以及原位替换后的 v6 练功房。

边界：不包含奖励、失败成本、战后资源回写、Redis、checkpoint、WebSocket、正式录像或普通任务 PVE。

专项设计见[`combat-v6 Phase 7B：权威人物构筑持久化与 v6 训练房接入`](./combat-v6-phase-7b-authoritative-training-design.md)。

### Phase 7C：Redis权威运行时与回放归档

已接入训练房：2小时运行态、revision CAS、可恢复Host、Redis终局Outbox、仅传battleId的MQ通知和PG独立v6回放。任务、奖励与Condition不在本阶段写入。

### Phase 7D：野生灵兽暗雷与Condition结算

> 状态：功能与新版战斗页面已交付（2026-09-06）；部署及故障恢复验收按专项记录逐项确认。

实现青溪灵草坡固定物种/等级池、点击必遇敌、角色每日20次额度、当前资源入场和独立Condition消费者。终局允许净回血回蓝，使用PG幂等凭据防止重复回写。捕捉、宠物实例、奖励及任务消费者留待后续；详见[专项稿](./combat-v6-phase-7d-wild-encounter-design.md)。

练功房与野怪页面已使用完全独立于 v5 的战斗布局、公共组件与会话播放控制器，继承 v5 的 HP/MP/护盾语义配色。阵容布局面向双方各四名人物及各自一只召唤兽；对齐展示 HP、叠加护盾、MP 与状态，具体数值和资源进入单位详情。指令收敛为攻击、神通、器诀、防御、保护及逃跑等一级入口，技能通过详情抽屉选择，说明复用独立 Tooltip。

展示协议使用 `playback.format: 'delta-v1'`：顶层保留最终完整单位状态，逐行动帧只携带公开字段差量和事件游标。客户端逐行动同步播报与单位状态，不提供整回合跳过；权威最新状态与玩家当前看到的状态分离。详见[战斗 UI 与展示协议](./combat-v6-battle-ui.md)。

上述阵容容量不代表已实现真实多人控制或持久召唤兽。当前 PVE Host 仍由单一玩家控制人物，其余单位采用 NPC 策略；人物与召唤兽的顺序操作随 Phase 9 的实际控制链路落地。已完成认证野怪胜利与资源结算回归；消息重投、进程中断、归档及解锁失败等故障场景须以实际验证记录为准，不据此宣布全部生产门禁通过。

### Phase 8：在线战斗、协议与录像

> 状态：Phase 8A 已接入实现并通过纯本地八账号 4v4 主链路（2026-09-06）；完整故障矩阵与响应式验收仍在进行，详见[擂台接入专项设计与验证记录](./combat-v6-phase-8a-arena-online-design.md)。

沿用 Phase 7C/7D 已交付的 Redis 运行态、revision CAS、可恢复 Host、v6 回放归档、公共单位视图、差量帧和战斗页面。已有 PVE 恢复能力不等于多人指令调度与故障恢复已验收，不重复建设一套展示协议或页面。

#### 技术职责

| 职责 | 锁定方式 |
| --- | --- |
| 提交指令 | HTTP，提供明确提交结果与幂等重试 |
| 下令状态、回合锁定、结果到达、截止时间 | WebSocket 推送 |
| 首次进入、断线恢复、事件缺口补齐 | HTTP 获取完整基准及增量；有匹配基准时补增量，缺失时取完整基准 |
| 文字播报、血条变化 | React 与播放控制器消费服务端提供的逐行动差量帧 |

WebSocket 负责及时送达战斗数据，React 与播放控制器负责渲染。按回合推送结果及其行动差量帧，不通过 WebSocket 高频同步每一帧画面。HTTP 与 WebSocket 交付的同一结果必须按战斗、回合、revision 和事件游标去重与校验连续性；推送不替代服务端持久权威状态，断线或缺口通过 HTTP 恢复。具体消息 schema、订阅鉴权、恢复衔接与载荷上限在 Phase 8A 专项稿锁定。

#### Phase 8A：擂台接入 v6 与服务端权威回合

目标：复用现有擂台房间与冻结阵容，在开战处接入 v6 引擎及新版战斗页面，保留每方最多四人的现有组局规则，验证各自下令、服务端独立推进，以及断线或进程重启后的正确继续。

交付与边界：

- 复用擂台房间、邀请码、准备、冻结阵容与开战机制，不新增邀请系统；开战桥接校验 v6 构筑和战斗互斥，锁定双方构筑，满状态入场，不回写持久 HP/MP，不发奖励。
- 控制权表达为“参与者 → 可控制单位”，支持现有擂台每方最多四名人物；不把在线 Host 写死为一个 `playerId`，不提前建设宠物接口。
- 服务端生命周期为“收集指令 → 锁定结算 → 播报阶段 → 下一回合”。收齐指令或达到截止时间后服务端自动推进，不依赖客户端再发 resolve 请求。
- 每回合下令窗口 30 秒，提交即锁定；所有仍在场的人物包括倒地人物均可提交。到其行动顺序时已复活才执行，顺序已过后复活不补行动；已逃离、永久死亡及未入场单位不收集指令。
- 超时默认普攻。短暂断线保留本轮下令窗口，到期补普攻；确认离线者后续回合自动普攻，重连后从尚未锁定的操作机会恢复手动。不增加认输、连续超时弃权或功能开关，离场复用逃跑。
- 100 回合仍未分胜负判平局，保留两小时绝对期限，绝对期限到达记超时终止。断线不暂停计时，刷新不重置截止时间。
- 指令以战斗 ID、回合 ID、操作单位与请求幂等键校验并原子收集；不能因另一玩家先提交、全局 revision 增长而拒绝本人的合法并发提交。全局 revision 继续用于状态同步和结算提交。
- 服务端按现有约每行动一秒的节奏提供统一播报结束时间与下一轮截止时间；客户端播放完成回执不作为全场推进前提。刷新或重连直接恢复最新权威状态并补齐战报，不强制重播离线行动；连续在线仍逐行动播放。
- 公共视图展示 HP/MP 比例、护盾、状态及提交状态，不暴露对手尚未结算的技能与目标。精确属性、私有资源及未结算指令仅本人可见；HTTP 恢复和 WebSocket 推送使用相同权限边界。
- 使用 v6 独立 resolver、快照和指令契约；可复用 Redis、消息、连接与通用监控基础设施，不直接接入依赖 battle-v5 类型的 `OnlineBattleResolverPool`。
- 训练房和现有野怪保持原有玩法语义，按需求逐步接入；本阶段不顺带强制迁移全部 PVE Host。
- 同账号多标签页或设备可查看，首个被服务端接受的指令生效，其余同步为已锁定；结果和本场回放双方可读，不开放播放器。
- 上线停机维护后直接硬切，不兼容或续跑旧擂台战斗，不保留新开战的 v5 回退链路。全量旧引擎与其他玩法写链路的删除仍按 Phase 10 范围处理。

不包含新组队系统、匹配、排位、跨服、公开观战、分享、复杂托管 AI、奖励、任务进度、道装经济、功法获取或持久召唤兽。

完成条件：

- 从 1v1 到现有房间允许的每方最多四人，认证参与者只能提交本人合法指令，私有数据不会通过查询或推送泄露给其他参与者。
- 并发提交、重复 HTTP 请求、重复/乱序推送、超时与最后一份指令竞争，不造成重复回合结算或丢失有效指令。
- 所有客户端断线后仍能按规则推进；服务端从已提交运行态及锁定指令恢复，不改变 RNG、事件与结果，不接受旧回合指令。
- 重连与事件缺口能通过 HTTP 恢复，正常在线客户端继续逐行动播放，播报完成回执不阻塞服务端。
- 客户端不计算权威伤害，切磋不修改持久资源或发放奖励；实际故障验证结果与未验证项明确记录。

#### Phase 8B：回放播放器与版本兼容

> 状态：新版战绩列表与回放播放器已接入（2026-09-06），浏览器交互验收待补，详见 [Phase 8B 专项设计与验证](./combat-v6-phase-8b-replay-design.md)。`/game/battle/history` 硬切 v6；旧记录读取与分享 API 返回 410。列表仅查询归档元数据及角色关联，不读取、解包回放 JSON。

新记录采用 `combat_v6_replay_v2`，保存开场基准、权威结算时采集的逐行动差量、冻结展示资料与终态校验数据。播放器复用 v6 阵容、战报、详情与配色，支持播放/暂停、前后行动、回合跳转和倍速。旧 v6 v1 归档只显示静态战报和结果；不重跑旧引擎。回放可见性限定本人关联角色，不包括观战或分享。

基于已归档的 v6 回放增加播放器，复用阵容、战报与单位详情展示组件。专项设计必须明确历史逐行动单位状态的来源、必要的回放格式版本提升和旧归档策略：不能从文字战报反推 HP/MP，也不能依赖当前最新内容表或最新规则重算旧录像。缺少可播放状态的旧归档不得伪造动态状态。

完成条件：新归档可稳定还原逐行动单位状态与事件，播放不改变权威数据；版本升级后旧记录仍按明确兼容策略读取。

#### Phase 8C：观战与在线负载验证

> 状态：房间观战席已接入，真实 1v1 加观众的开战、实时播放、重新入席、重连、终局与历史权限拒绝复验通过；4v4 观战负载和故障矩阵仍待完成，详见 [8C 专项设计与验证记录](./combat-v6-phase-8c-spectator-design.md)。沿用邀请码进入，只读公共视图、实时逐行动播放；观众不获得历史权限。

真实 4v4 人物控制与基础验收已提前至 8A；8C 在此基础上开放只读观战，扩展观战权限、在线并发与推送负载验证。持久召唤兽与换宠控制仍进入 Phase 9，页面的 16 单位展示能力不替代真实参与者验收。

Phase 8 整体完成条件：

- 客户端不计算权威伤害。
- 服务端能从 checkpoint + 后续指令继续战斗。
- 录像不依赖当前最新内容表重算。
- 多参与者与观战拥有明确的权限、同步和恢复契约。

道装持久化及经济外围、功法获取与改修、宗门养成可操作入口仍按各自专项范围交付；已有编译能力或持久容器不等于养成闭环完成，也不自动并入 Phase 8A。

### Phase 9：召唤兽与后续梦幻式系统

召唤兽按[`combat-v6 召唤兽系统设计`](./combat-v6-summoned-beast-system-design.md)建立独立持久领域。首版采用独立五维与五资质、0～180 等级的妖类境界映射、固定出生技能格、随机覆盖打书、6 只携带与单宠在场，并实现死亡扣除 50 寿命的最小闭环。人物功法与兽诀不可混为同一实例类型；首版不开放召唤兽修炼。

#### Phase 9A：持久召唤兽与双操作闭环

> 状态：代码已实现（2026-09-06），已完成本地领取、野外带宠战斗与回放、双账号带宠切磋的基本验证；完整多人及故障验收待完成。详见 [9A 实施与验收记录](./combat-v6-phase-9a-summoned-beast-plan.md)。Phase 8 的 4v4 观战负载、多实例与故障验收继续单独追踪，不因 9A 实现而标记通过。

建立正式召唤兽个体与独立编组，提供一次性新手领取、最多携带六只和单宠在场；接入 PvE、练功房与擂台，完成先人物后召唤兽的整组指令提交、召回换宠、公共观战和统一归档。每场满 HP/MP 入场，不持久保存宠物当前 HP/MP；有效 PvE 死亡每宠每场幂等扣除 50 寿命，擂台与练功房不扣寿命，并提供确定性寿命恢复出口。

倒地人物仍需提交允许的操作，并可指挥存活宠物，但不能预选召唤或换宠。超时／离线对人物与可行动宠物分别补普攻，不自动换宠。换宠时仍可预先提交旧宠指令，按速度顺序执行；旧宠行动前若已退场，则不执行。新宠下一回合才行动。

#### Phase 9B：正式获取与基础成长

> 状态：代码已实现（2026-09-06），基础本地捕捉、成长、加点、放生与回放流程通过；付费休养及完整故障矩阵尚未验收。见 [实施与验收记录](./combat-v6-phase-9b-capture-progression-plan.md)。

野外捕捉、经验升级、自由加点，以及获取和恢复资源闭环。沿用 9A 正式个体模型，不重新生成已持有个体的成长、资质或固定技能格。

捕捉复用施法、执行时 MP 支付和合法目标转向，MP 随物种携带等级线性增长。携带等级限制捕捉与出战，个体战斗等级超过人物时禁止出战和培养。胜利经验仅发给终局在场且存活的已有灵兽，不分摊。持有 24、携带 6，放生无收益；灵石休养，不开放洗点。

#### Phase 9C：统一物品系统与兽诀培养

> 2026-09-07：代码已实现；本地自然掉落入包、存取、学习、下一场连击生效及旧回放不变的闭环通过，并修复结算后刷新丢失奖励提示。满包发放与装卸已通过本地测试物品验收；自然掉落满包分支、有数据装备迁移、越权及故障矩阵仍待验收。见 [9C 实施与验收记录](./combat-v6-phase-9c-inventory-plan.md)。

建立 v6 唯一物品持有路径，直接使用角色 `cultivator_id`，以 `location` 区分 40 格背包和无限储藏室，不建立容器表。兽诀每格 99 本，战斗溢出奖励进入新储藏室。旧玩法库存保留为后续洞府储藏室来源，本阶段不迁移或兼容读取；未来取出时原子转换进入新系统。v6 当前装备等物品仍须接入新持有路径。

兽诀获取、事务化随机覆盖打书、普通／高级同系生效规则。固定技能格不增长，不加入指定覆盖、锁定或保底。并发使用角色 Redis 锁、数据库事务和 revision，不新增消费操作凭据表，响应不明通过刷新权威状态恢复。

#### Phase 9D：道装图纸与新版炼器室

> 状态：实现完成，核心本地链路已验收；剩余项见 [9D 铸造设计与验收记录](./combat-v6-phase-9d-forging-plan.md)。承接 4C 的获取与炼器范围，不包含完整装备经济外围。

新增按槽位、等级定义的道装图纸，将兽诀等物品定义独立分类管理。新版炼器室使用图纸、天地灵气、灵石及 1～5 件合格材料合成，公式随图纸等级固定增长；首轮无 AI，名称使用预设池。炼器只消费统一物品栏材料，旧材料通过宝库取出转换，不进行多数据源读取。

材料增益已确认采用额外抽样取高、同类叠加触发概率，每件 1.8 个百分点、同组最多 9%：矿石影响白字数值，辅助／妖兽材料影响附加属性数值，天材地宝仅影响器蕴数量概率；器诀保持独立随机。图纸不得高于人物等级，满足条件必定产出一件且固定已鉴定。灵石、灵气、材料数量与品质成本按专项稿固定公式执行。图纸掉落与通用掉落池暂缓，以仅开发环境开放、无鉴权的受限发放接口准备本地测试物资。

#### Phase 9E：通用掉落底座与野外接入

Phase 9E 已实现通用掉落底座、库存堆叠键与野外首次接入，核心本地链路通过，详见 [9E 设计与验收](./combat-v6-phase-9e-drops-plan.md)。底座不依赖战斗、角色或库存；野外按胜利场次抽取一次，范围与概率由配置指定，不对外展示概率。其他业务尚未接入，未完成验收仍单独记录。

#### Phase 9F：功法玉简与人物道印养成闭环

> 2026-09-07：实现完成，核心本地链路已验收；并发、故障、真实满包及完整 PvP 回归仍待验证。见 [9F 方案与验收](./combat-v6-phase-9f-manuals-plan.md)。

复用 5A 功法规则与现有持久状态，玉简进入统一库存与野外掉落；完成参悟、指定改修、本篇原位升真解及无返还散功。原悟道功法入口硬切，关闭旧功法创建写入。只消耗储物袋中一本玉简，无额外费用；活动战斗期间禁止修改，下一场生效。无 AI、旧数据转换、多构筑、残页或交易。

#### Phase 9G：宗门心法与经脉养成闭环

> 代码已落地，本地养成主链路及携带道印／装备转宗、转回历史玉牒已验收。两宗门符箓已硬切 v6；高层解锁与完整多人战回归仍见 [9G 计划与验收](./combat-v6-phase-9g-sect-plan.md)。

硬切现有宗门战斗养成入口，开放六心法升级、共用七层经脉深度、两流派各一套节点方案及战斗外免费调整。旧流派取最高已解锁层级进行一次性 SQL 迁移，保护已有 v6 进度，不相加、不返还、不覆盖新版节点。转宗继承心法等级、深度、道印和装备，重新选择目标宗门节点。

#### 战斗药品与物品指令：延后开发

> 2026-09-07 用户确认：暂不开发战斗中使用道具（药品），不作为下一阶段实施内容；此前提出的 Phase 9D 药品方案仅为讨论建议，未锁定。

本次不开放物品战斗指令，也不实现药品获取、战斗消费结算或相关页面。统一库存继续保留现有能力，不为延期玩法预埋字段、接口或功能开关。未来重新立项时，再确认药品类型、人物／灵兽使用权限、各玩法消耗规则和异常结算边界。9C 其余验收按用户决定暂缓，未验证项继续保留，不标记为通过。

后续再按需求增加：

- 宗门怒气技及其他明确需要的扩展；4B 已交付的战意与器诀不重复建设。
- 药品与物品指令（已明确延期，重新立项后再排期）。
- 阵法。
- 其他已明确排期的战斗系统。

### Phase 10：切流与退役 v5

#### Phase 10A：秘境战斗与奖励链路切换 v6

> 2026-09-08：秘境 v6 战斗、统一掉落累计及最终发放已落地；本地五轮通关、逃跑、刷新恢复及战绩已验收。专项首轮补验通过满包溢出、并发请求拒绝、客户端中断后结算重试、胜利后离开、普通退出、未满级灵兽经验及死亡寿命／后续场次排除。真实战败、服务进程中断、换宠及恢复丹药等仍待补验，详见 [10A 实施与验收记录](./combat-v6-phase-10a-dungeon-plan.md)。

所有现有秘境战斗入口硬切 v6，保留探索叙事与胜利后的继续／离开选择，复用新版战斗页面、持久灵兽及统一归档。首轮采用少量按等级配置的遭遇模板，不开放捕捉；失败或成功逃跑结束秘境，保留此前已确定收益，当前失败战斗及未通关不发对应奖励。

秘境全部新物品奖励接入通用掉落及新版储物袋，AI 不决定经济收益，旧评级保底材料规则替换。累计物品与人物／灵兽经验最终统一发放，灵兽资格按每场终局在场且存活记录；HP、MP、寿命逐场结算。整段秘境禁止构筑修改，允许战斗内合法召唤／换宠。探索恢复使用 v6 上限，不新增旧伤势、战斗药品、多人秘境或功能开关。本阶段不等于全仓 v5 已可删除。

#### Phase 10B：蜃楼幻境 v6 独立肉鸽挑战

> 2026-09-08：v6 独立幻境、Redis 状态、祝福、直接发奖和页面硬切已实现。补充纯本地 dev 角色调整及灵兽发放，真实 20 层通关、奖励入包/满包溢出、同周重开、换宠、胜负、百回合平局、逃跑、API 重启、事务回滚重试及 Redis 丢失恢复通过；真实跨周、提交后 Redis 写失败窗口及 Profiler 定量检查仍待验证。首轮奖励配置及完整记录见 [10B 设计与实施计划](./combat-v6-phase-10b-tower-plan.md)。

保留金丹门槛、20 层、周周期、里程碑及周榜，硬切 v6 Host、公共战斗页面与统一归档。入场冻结构筑，人物与合格携带灵兽初始化满 HP/MP，后续场次继承局内消耗；祝福只影响本次挑战。离场不回写现实 HP/MP，不扣灵兽寿命、不发灵兽经验。允许合法换宠，死亡灵兽本次挑战不再出战。

使用配置化 v6 敌人与祝福，不转换旧敌人或引入 AI 数值。里程碑通过通用掉落及资源路径直接发放，满包转储藏室。按用户确认，挑战、周进度、领取标记和榜单使用 Redis，不新增挑战或周进度表；正常运行同角色同周同里程碑只领一次，数据丢失允许重开及重新获得奖励。奖励仍写现有数据库，不承诺数据库与 Redis 跨存储严格只发一次，不增加持久补偿机制。单人战斗继续使用 HTTP 与客户端差量播放。不增加多人、捕捉、战斗药品或功能开关，也不在本阶段删除全仓 v5。

#### Phase 10C：天骄榜挑战切换 v6

> 2026-09-08：已实现，纯本地三账号完成胜负、百回合平局、首发灵兽、名次、每日限次、跨境界、并发、归档失败恢复及回放权限验收；全量 2028 项共享测试、Lint、构建通过。真实跨日、提交后 Redis 确认失败窗口和满榜压力仍待验证。详见 [10C 实施与验收计划](./combat-v6-phase-10c-ranking-plan.md)。

保留异步自动挑战、每日 10 次和原榜单规则，双方使用当前构筑的独立满状态副本及合格首发灵兽，不影响现实资源。统一回放授予双方权限，列表仅元数据。Redis 预占次数、保存可恢复挑战，依据最新榜单原子结算；数据丢失接受次数及榜单重置，不新增挑战记录表。上线停机定向清空旧天骄榜排名与当日次数，按 v6 重新争榜。赌斗、宗门任务、其他榜单和全仓 v5 删除不纳入。

#### Phase 10D：旧赌战全面下线

> 2026-09-08 用户决定：赌战玩法后续重新设计，旧版全面下线并删除专属代码，不进行 v6 平移。新版玩法另行讨论。

删除旧赌战页面、导航、API、应用服务、仓储、过期任务、后台命令及专属传闻事件。保留旧数据库表及 schema 作为历史托管资产核对依据，停止运行时读写及自动清理；不执行 DROP、不迁移旧战绩、不新增功能开关。停机时需核对和退还旧待应战押注，处理赌战专属积压消息，详见 [10D 下线记录](./combat-v6-phase-10d-bet-retirement.md)。本阶段不包含新版赌战、宗门任务迁移或全仓 v5 删除。

#### Phase 10E：统一自动托管与宗门任务战斗迁移

> 2026-09-08：实施与验收中。10E-1 已改为客户端 3 秒计时、服务端单次 AUTO 选招。10E-2 四类任务已接入 V6 对手冻结、可恢复会话、公共操作页、资源互斥及事务结算；跨期活动战斗保留，旧任务定向撤销工具已提供。运行证据与未验收项见 [10E 设计与验收计划](./combat-v6-phase-10e-auto-sect-tasks-plan.md)。本阶段尚未完成全部验收。

10E-1 在所有公共 v6 可操作战斗面板提供一级「自动／取消自动」按钮，统一覆盖练功房、野外、秘境、幻境、擂台及新宗门任务，不设置按玩法开放的功能开关。客户端等待 3 秒后提交 AUTO，服务端固定策略为本人及在场灵兽选招。每场及刷新后默认手动，离页停止后续请求；取消不撤回已发请求。擂台只代交本人指令，仍遵守多人收令和超时规则。回放、观战无操作权限；天骄榜保持既有自动结算。

10E-2 将矿脉巡逻、每周演武、外宗悬赏和长老试炼切为可恢复的 v6 手动回合战斗，复用托管、公共页面和统一归档。保留既有任务领取、额度、奖励及晋升规则；演武隔离现实资源，其余结算人物 HP/MP 和灵兽死亡寿命，不新增灵兽经验或战斗掉落。已开战任务过期仍可完成，过期后不能新开或重试。维护期定向撤销未完成的旧战斗任务并恢复占用额度，保留已完成待领奖任务。

本阶段不包括非战斗宗门任务重做、突破试炼迁移、全仓 v5 删除、战斗药品、捕捉、自动换宠或自动连续挑战。突破任务仍有旧战斗执行路径，须在后续退役盘点中独立处理。

#### Phase 10F：突破试炼战斗迁移 V6

> 2026-09-09：六种突破试炼已完成真实胜利、资源结算及可播放归档；同时验收落败、逃跑、百回合平局、召回／召唤／换宠、刷新与离页停自动、旧 revision 拒绝、真实事务失败后重试及正式突破入口。修复三项运行问题，lint、build 和 10 项突破纯逻辑测试重新通过。普通配装难度、部分恢复与互斥边界仍待专项验收，不标记整阶段全部通过。详见 [10F 开发边界与验收计划](./combat-v6-phase-10f-breakthrough-plan.md)。前序阶段未验收项继续保留。

心魔劫、化神之扰、法则试锋、雷劫淬体、大执念劫和天劫前奏全部切为正常 V6 普通战斗，人物与灵兽遵循公共参战、召唤和换宠规则，不设置试炼专属参战限制。复用公共手动回合页面、客户端 3 秒 AUTO、可恢复会话、现实资源结算与统一回放；不新增经验或掉落。

心魔类使用原生 V6 人物构筑镜像，其余采用配置化 NPC。每次开战冻结对手，同场恢复不变，重新挑战按新构筑生成。胜利只完成对应任务目标，保留前置准备与最终突破条件；失败、逃跑和平局可重试。活动及待结算期间阻止构筑修改、互斥战斗和最终突破，终局资源、任务目标与归档保证事务及幂等。

保留已完成旧试炼目标，不重做突破任务体系，不新增战斗药品、后台调度或通用任务框架；旧后台模拟器在 10G 下线，新版模拟器重做及全仓 V5 删除另行安排。

#### Phase 10G：旧后台战斗模拟器全面下线

> 2026-09-09 用户决定：后台战斗模拟器后续重做，本版全面下线清理，不进行 V6 平移。

删除后台模拟器页面、导航、前端路由、单场／蒙特卡洛 API、专属服务和请求响应契约，以及仅供模拟器使用的角色随机抽样查询。不保留隐藏入口、空页面、兼容接口或开关。该服务即时计算并返回结果，没有专属持久化表或后台模拟任务，无需数据迁移或清理历史战绩。

保留仍有调用方的 V5 执行器、敌人生成器、旧在线战斗及共用展示代码，不借本次下线扩大删除范围；新版模拟器单独讨论。下一阶段先核对剩余 V5 依赖，再确立全仓退役边界。前序阶段未验收项继续保留。

验收：源代码无模拟器入口及专属符号残留，旧单场／批量接口不能执行模拟，后台导航正常且无模拟器入口，旧页面地址进入既有未匹配路由处理；lint、build 及 diff 检查通过。不新增服务端或前端测试。

2026-09-09 已实施并完成上述验收：本地 POST `/api/admin/battle-simulator/duel` 和 `/monte-carlo` 均返回 404「接口不存在」；真实浏览器以 local1 管理员会话打开后台总览，侧栏及模块入口均不再展示模拟器，旧 `/admin/battle-simulator` 地址显示既有 404 页面。`bun run lint`、`bun run build`（含前后端类型检查）、`git diff --check` 通过；构建保留既有 chunk 提示。本次仅下线专属功能，未修改共享引擎逻辑，未运行全量 shared 测试。

#### Phase 10H：旧 V5 在线战斗整体下线

> 2026-09-09 用户确认：停机维护后硬切，旧在线战斗及专属消费者全部删除，不考虑旧场次待处理数据，不做排空、迁移、补偿或兼容运行。

已删除旧邀请大厅与战局页面、导航、客户端同步与 Phaser 表现组件、专属样式及图片；删除 `/api/battle-matches` HTTP／WebSocket 和 `/api/admin/online-battles` 诊断入口、旧匹配／邀请服务、Redis 战局运行时与索引／票据、调度和结算 Worker。

旧回合结算、终局清理、回放归档的消息发布者及消费者全部删除，连同启动／关闭、健康检查、NATS stream／consumer 注册、重试及修复调度代码一起移除。构建不再生成 online-battle-resolver.js，旧 arena-v6-cutover 维护脚本删除。不为旧数据保留消费者或补偿逻辑。

V6 擂台仍使用的 ArenaRoomService、ArenaBattleStartOrchestrator、NATS 共用连接、领域事件、combatV6Messaging 和 V6 回放归档保留。旧 PostgreSQL 回放表仅保留 schema 与历史 JSON 类型，删除读写仓储、协议解析及定期清理，不执行 DROP 或历史数据修改。既有 NATS 持久 stream／consumer 对象不会因删除代码而自动销毁；本次不操作部署环境的存量对象，新进程不再注册或订阅它们。上线时由停机维护隔离旧进程，不能让旧版本实例继续运行。

验证通过：`bun run lint`、`bun run build`（含前后端类型检查）、`bun run test`（222 文件／2043 项）、`git diff --check`。已删除三份仅测试退役协议／表现的测试；共享 V5 引擎测试保留。构建保留既有 chunk 提示。

本地运行验收：旧创建、邀请、后台 metrics 接口返回 404；消息健康检查 redis/nats/messaging 均 up。真实浏览器验证 V6 擂台创建房间、准备状态及退出，临时房间已释放；旧 `/game/battle/live/:matchId` 显示 404。旧大厅 `/game/battle/live` 不再匹配邀请页，而由既有 `battle/:id` 回放路由显示 Invalid UUID，不再具备任何旧战斗功能。本轮未重跑完整多人战斗、自动、观战及新终局归档；前序验收余项继续保留。

后续阶段再迁移角色展示、condition／恢复等共用能力中的 V5 依赖，然后删除剩余引擎；本次在线链路下线不等同于全仓 V5 已退役。

2026-09-09 补充决定：历史在线回放、赌战及 V3 战绩表本版标记弃用，本次退役版本上线后的下一版本删除表与 schema。V3 战绩表仍需先解除聊天分享及定期清理引用；V6 表不在删除范围。具体清单、外键顺序与验收要求见 [历史表删除计划](./combat-v6-legacy-table-retirement.md)。本版不执行 DROP。

#### Phase 10I：人物面板与现实资源统一 V6

2026-09-09 用户确认实施。HUD、角色属性、加点／洗点展示、创建预览和他人属性改用 V6 面板，初始化、自然恢复、灵泉与丹药上限统一 V6。永久六维、恢复倍率、药效成本和非战斗规则保留；不删除历史表或扩大为全仓 V5 退役。

已实施服务端权威面板、资源视图失效刷新、上限变更时按旧上限结算过去恢复的事务处理，移除提高上限自动补满逻辑。正式加点即时刷新、灵泉恢复、训练入口面板一致性和 360px 属性页面已通过本地运行验收。无宗门页面、真实丹药／洗点及构筑切换等补充运行验收仍待完成，详见 [10I 实施与验收记录](./combat-v6-phase-10i-character-panel.md)。不将主体实施完成等同于所有场景已验收。

#### Phase 10J：旧战斗展示与分享消费者退役

2026-09-09 用户确认实施。洞府近期战绩复用 V6 战绩接口及列表，展示最近三场并进入现有回放；世界与宗门聊天停止创建旧战报分享，历史消息保留内嵌摘要并显示“旧版战报已停用”。删除旧播放组件、宗门旧战斗结果渲染器、V3 仓储、旧战斗协调／投影入口及 V3 定期清理。

主体实施与重点运行验收完成：lint、build、223 文件／2049 项共享测试通过；首页三条记录及回放、两个聊天入口的 410 下线响应通过本地验证。历史聊天样本及其他未执行场景以 [10J 实施与验收记录](./combat-v6-phase-10j-legacy-battle-consumers.md)为准。三张弃用历史表已无运行时消费者，仍按下一版本删除，不执行 DROP；共享 V5 引擎及 creation-v2 的剩余依赖另行退役。10I 补充验收待办继续保留。

#### Phase 10K：旧宗门战斗体系退役

2026-09-09 用户确认实施。删除五宗门旧编译器、构筑器、AI 策略、节点效果及旧养成内部服务和写入口；宗门模块保留组织功能与历史静态元数据，V6 战斗内容和养成保持独立。旧任务目标解析剥离 V5 combatant，七个旧养成 HTTP 入口继续返回 410。不改 schema 或删除历史表，不扩大为全仓 V5／creation-v2 退役。

主体实施与重点运行验收完成：lint、build、191 文件／1412 项共享测试通过；五宗门接口读取、真实矿场任务自动战斗／幂等奖励／会话释放／归档列表、六心法研习、经脉展示及转宗预览通过本地验证。测试数减少来自退役专属测试删除。新角色入宗、实际转宗、非空历史迁移与其他未运行场景见 [10K 实施与验收记录](./combat-v6-phase-10k-sect-combat-retirement.md)。10I／10J 补充验收待办继续保留。

#### Phase 10L：旧造物生产退役与炼丹物品栏接入

2026-09-09 业务代码已实施，炼丹、丹方、宝库取出、新版丹药使用及满包溢出已完成本地运行验收。炼丹保持玩法与生成规则，迁出旧造物耦合代码，只消耗新版 bag 材料，丹药直接进入新版物品栏并接通使用。旧材料、丹药及消耗品保留为洞府宝库，通过统一协议取出；旧功法、神通、装备生产代码全面下线，历史数据和表结构保留，邮件旧造物仍发放到旧表。炼丹界面复刻新版炼器结构并使用新炉图。创角、新手奖励同步停止新增旧战斗产物。实施内容、验收证据和未覆盖项见 [10L 记录](./combat-v6-phase-10l-alchemy-inventory-plan.md)；历史旧战斗产物邮件缺少本地样本，符箓专属流程等尚未逐项运行验收。

#### Phase 10M：历史造物读取解耦与 V5 引擎退役

2026-09-09 已按确认边界完成代码实施：删除 V5 与 creation-v2 引擎及专属消费者，历史物品改为存档直读，关闭旧装配和直接删除入口，退役旧装备／功法／神通榜单；保留历史数据、schema、原邮件发放路径和丹药榜。lint、两端构建、125 文件 / 844 项共享测试通过，主要读取与停用 API、本地列表页面已检查。真实旧物详情与历史附件领取因本地无样本仍未验收，不将本阶段标为全量验收完成。详见 [10M 实施与验收记录](./combat-v6-phase-10m-legacy-engine-retirement.md)。

#### Phase 10N：NPC 回收与新版丹药榜

2026-09-10 主体实施与重点运行验收完成：回收改为固定 NPC 对话、完整公共物品栏、多选按份报价与事务成交，只消耗 bag 中的材料、丹药；旧库存先取出再出售，旧法宝回收保留折叠入口。丹药榜切为新版 bag + storage 当前持有事实，同角色同事实合并排名，拆堆不占多条。lint、两端构建、126 文件 / 848 项共享测试通过；实际成交、数量调整、并发和重复提交、失效重报、旧宝库取出、丹药拆堆及退榜已运行验证。真机触屏、故障注入、历史法宝等未覆盖范围见 [10N 实施与验收记录](./combat-v6-phase-10n-npc-recycle.md)。

#### Phase 10O：坊市物品栏交互与购入链路迁移

2026-09-10 主体实施与重点运行验收完成：普通坊市切为左商品格、右五列40格公共物品栏，移动端背包抽屉，多选统一购买；普通材料和黑市成交物直接进入 bag，满包进入 storage，该阶段种子仍入宝库供灵田播种（已由后续10P迁入新版库存）。黑市交互保持原样，仅补充入库去向。复用玩家命令凭据补齐购买请求与逐商品幂等，扣款／整批入库同事务，保留原价格、刷新和额度规则。真实购买、满包溢出、种子播种、黑市成交、并发防重及回收闭环已验收；lint、构建与127文件／851项共享测试通过。跨期／故障、高额确认、全部节点与真机触屏等余项见 [10O 实施与验收记录](./combat-v6-phase-10o-market-inventory.md)。不迁移灵田其余链路、玩家交易、邮件或历史表。

#### Phase 10P：灵田与新版物品栏闭环

2026-09-10 主体实施与重点本地运行验收完成：坊市和初始种子进入 seed.v1，播种／道具培育仅消耗 bag，材料和灵果收获优先入 bag、满包进 storage。历史种子显式取出可播种，历史作物可续养收获，灵果按原效果食用；保留种植玩法、成本、成长和产出规则。页面采用左田块操作、右五列40格完整公共物品栏，移动端抽屉。lint、类型检查、构建及127文件／853项共享测试通过。完整并发／故障矩阵、全成本组合、收获后实际炼制回收与真机触屏尚未全验，详见 [10P 实施与验收记录](./combat-v6-phase-10p-spirit-field-inventory.md)。邮件、交易、补偿、删表及自动种植不在范围内。

#### Phase 10Q：邮件附件与赠送接入新版物品栏

2026-09-10 主体实施与重点本地运行验收完成：新版 inventory_v1 邮件附件领取优先 bag、满包 storage；历史附件继续洞府宝库。赠送仅消耗 bag，每封一个堆叠，覆盖材料、种子、丹药／灵果、图纸、玉简、兽诀和未装备道装，保留好友条件与传音符成本。写信左表单附件格、右五列40格公共物品栏，窄屏抽屉；删除赠送对旧拍卖服务的依赖。新系统奖励调用点接入独立生产入口，拍卖邮件保持旧协议。双角色顺序登录赠送领取、混合附件、满包溢出、版本／数量／越权拒绝及并发防重已运行验证；lint、前后端构建与128文件／857项共享测试通过。各系统奖励实际触发器、历史旧装备附件、故障矩阵及真机触屏仍未全验，详见 [10Q 实施与验收记录](./combat-v6-phase-10q-mail-inventory.md)。不迁移拍卖、补偿、批量历史转换或删表；前序未验收项继续保留。

#### Phase 10R：拍卖寄售与新版物品栏闭环

2026-09-10 主体实施与重点本地运行验收完成：新寄售仅从 bag 扣物，复用货单表保存带版本的可信物品事实；成交与下架／过期退回使用 inventory_v1 邮件，领取优先 bag、满包 storage。有品质材料、种子、丹药／灵果保留玄品起售与品质限价；用户确认无品质的道装、图纸、玉简、兽诀全部开放，沿用全局单价上限，不虚构品质。保留五单、48小时、一口价、阶梯税、好友专属及原符箓成本。寄售改用左表单、右完整公共物品栏与窄屏抽屉，解除旧物品表依赖，补齐上架版本校验与持久防重。双角色部分成交、各主要类型入库、下架退回、专属符箓、并发防重、满包及实例事实已本地验证；lint、两端构建与129文件／863项共享测试通过。自然到期、第三账号专属权限、故障矩阵及真机触屏余项见 [10R 实施与验收记录](./combat-v6-phase-10r-auction-inventory.md)。用户在上线前停机用旧版本下架返还所有旧货单；新代码不实现旧货单兼容或维护转换。不含竞价、求购、召唤兽交易、补偿或删表；前序未验收项继续保留。

#### Phase 10S：宗门交付与兑换接入新版物品栏

2026-09-10 主体实施与重点本地运行验收完成：材料（含固定掉落）、丹药与道装交付统一只消耗bag；道装要求新版部位、最低装备等级和未装备，不虚构品质或完美词条。用户确认需求以已开放产出档位封顶，当前最高10级，高阶掉落不在本阶段扩展。宗门兑换与采矿材料奖励直接进入新版库存，满包进入storage；保留任务权限、周期、奖励、贡献价格与限购。交付改为公共完整物品栏，增加revision校验与失败重试防重，解除旧材料／法宝交付依赖。真实道装、丹药、材料交付、固定材料多堆、兑换入包／溢出、越权／超量拒绝与幂等已本地验证；lint、两端构建与129文件／871项共享测试通过。用户授权维护期撤销未完成旧法宝委托、恢复次数，清除失效要求并保留已完成任务奖励与交付档案；独立维护工具仅本地只读盘点，未执行实际清理。维护应用、跨期／故障、采矿实际奖励及真机触屏余项见 [10S 实施与验收记录](./combat-v6-phase-10s-sect-inventory.md)。不改宗门战斗、养成、设施，不补偿、批量转物或删表；前序未验收项继续保留。

#### Phase 10T：世界／宗门聊天道具展示接入新版物品栏

2026-09-10 主体实施与重点本地验收完成：两频道晒物统一读取本人新版bag，支持已装备道装，校验revision并保存发送时的公开物品快照；保留频道权限、冷却及附言长度，不扣物、不虚构品质。桌面完整公共背包与窄屏抽屉接入，消息复用新版只读详情；旧晒物保留记录与附言，提示停用并删除旧详情解析，无历史消息迁移或清空。世界道装／材料、宗门图纸／玉简、360px选物、陈旧版本／storage／异主拒绝及转存后快照不变已本地验证；lint、类型检查、两端构建与130文件／874项共享测试通过。逐类发送、独立接收角色、真实旧消息、故障及真机触控余项见 [10T 实施与验收记录](./combat-v6-phase-10t-chat-inventory.md)。角色查看与秘境叙事旧装配读取留待后续阶段，前序未验收项继续保留。

#### Phase 10U：角色查看统一 V6 构筑与旧装配读取退役

2026-09-10 主体实施与重点本地验收完成：排行榜查探与本人总览统一V6公开面板、六部位道装、装配功法、宗门流派和可用技能；默认只读一致性事务，不返回私有构筑档案或经脉方案。移除player.loadout协议、订阅、读取和事件，闭关／客栈／消耗品停止查询旧装配，删除无调用方的旧秘境提示词读取。用户补充确认取消闭关旧功法品质倍率，后续另行设计；本阶段删除倍率表与计算，其余收益因子不变。本人／他人页面、360px布局、换装更新、真实闭关与客栈恢复已本地验证，lint、类型检查、两端构建与131文件／877项共享测试通过。未初始化、非空功法、服药、故障等余项见 [10U 实施与验收记录](./combat-v6-phase-10u-character-build.md)。历史宝库／详情／回收保留，不补偿、不删表；前序未验收项继续保留。

#### Phase 10V：旧秘籍抽取退役与万界商行新版入库

2026-09-10 主体实施与重点本地验收完成。用户确认旧抽取先下线，已有专属符箓和秘籍暂不转换、不补偿，后续独立设计；删除专属页面、入口、API、服务和概率配置，历史符箓保留停用展示。万界商行改为新版库存发放，满包进入storage，保留价格与周限购；扣声望、入库、购买记录同事务，补齐持久请求防重。旧产物禁售及新奖励生产校验不影响历史邮件读取路径。真实兑换、满包溢出、入包、重放、限购及停用入口已验证，lint、类型检查、两端构建和131文件／876项共享测试通过。历史领取与逐类兑换等余项、维护配置清单及共用材料生成器中仍存旧秘籍类型的审计边界见 [10V 记录](./combat-v6-phase-10v-reward-inventory.md)。本阶段不等于所有现役产出已收口；不补偿、不迁移或删表，前序未验收项继续保留。

#### Phase 10W：V6核心链路验收与停机切换准备

2026-09-10 用户确认后，重点本地验收与停机操作单已完成，完整发布验收仍待收口。实际验证功法学习与流派冻结、训练刷新恢复、道装装配与版本冲突、历史丹药邮件经宝库进入新版库存后消费、野外胜利异步结算及归档、宗门战斗领奖与重放防重、未登录和异主物品拒绝；本轮战斗占用均已释放。lint、类型检查、两端构建与当前114文件／759项共享测试通过。未发现需要修改业务代码的缺陷；旧秘籍材料类型、现有生成能力及权重按用户补充决定保留，修正10V维护说明，不增加兼容代码。两个宗门维护脚本本地dry-run均为0条，不视为真实旧任务撤销通过。首次初始化、双人擂台、正式突破成功、故障与经济余项、目标环境迁移及真实维护数据仍是待验门槛，不能据此放行生产。详见 [10W验收记录](./combat-v6-phase-10w-release-acceptance.md) 与 [停机切换操作单](./combat-v6-phase-10w-maintenance-runbook.md)。未执行迁移、生产维护、删表或部署。

#### 后续切流与退役条件

切流条件：

- 目标玩法全部有 v6 Host。
- 玩家角色投影、修炼、至少所需宗门、装备和功法均已迁移。
- 已确认的旧货单返还、旧托管处置及宗门任务维护完成；不对停用秘籍与专属符箓做转换或补偿，旧秘籍材料生成能力与权重保留。
- v6 监控、战斗记录和错误诊断可用；停机维护后硬切，不增加回滚功能开关。

切流后：

- 禁止新建 v5 战斗内容和旧造物战斗产物。
- 旧战绩读取及分享已由 8B 退役，不保留旧记录查看入口。
- 逐一核实尚存旧玩法、角色投影等依赖后删除 v5；不能将战绩硬切等同于全仓旧引擎已可删除。

---

## 14. 质量门禁

### 14.1 共享引擎测试

测试只放在 `src/shared`。至少覆盖：

- core 指令锁定、速度顺序、状态、死亡/倒地和 RNG。
- rules-daoyou 物理、法术、固伤、治疗、封印和修炼差公式。
- 角色基础投影黄金值。
- 五轨修炼迁移的等级/进度原值保持、位阶晋升和幂等性。
- 装备属性白名单与冲突。
- 功法普通/高级冲突和覆盖。
- 宗门心法等级到技能等级映射。
- 经脉编译顺序和 patch 冲突。
- 战斗快照、事件流和版本戳稳定性。

### 14.2 架构测试

必须自动检查：

- combat-v6 不导入 battle-v5 / creation-v2。
- core 不导入 rules/content/projection。
- 内容表不含不可解析的技能、状态和引用。
- 所有技能/状态 ID 在所属版本内唯一。

### 14.3 交付检查

每阶段完成时至少执行对应的 focused tests；跨领域修改再执行：

```bash
bun run lint
bun run test
bun run build
```

文档或纯设计阶段只需检查 Markdown、链接、`git diff` 和代码事实，不为了形式运行全量应用测试。

---

## 15. 明确不采用的迁移方案

以下方案会让后续切换继续背负 v5，因此明确否决：

1. 长期通过 battle-v5 `AttributeSet` 计算 v6 面板。
2. 编写 `AbilityConfig -> SkillDef` 通用翻译器迁移旧技能、功法和装备。
3. 让同一份宗门定义同时生成 v5 与 v6 战斗产物。
4. 将 v6 派生面板写回 `Cultivator.attributes` 或数据库角色列。
5. 把四修炼等级绑定宗门心法，导致转宗时修炼变化。
6. 为了保留旧炼体战力，把旧 modifier 与新修炼效果同时挂载。
7. 在 core 内为特定宗门、经脉、装备或功法写 ID 分支。
8. 一次性重做所有宗门后再验证内容 DSL。

---

## 16. 当前未锁定的平衡项

这些问题不阻塞架构实施，但进入相应 Phase 前必须补充版本化决策：

- 是否保留境界压制和个体暴击抵抗。
- 首批功法谱系、本篇/真解数值、玉简掉落与残页兑换成本。
- 首个纵切宗门及其六心法定义。
- 愤怒、特技、药品、阵法和召唤兽的具体实施排期。
- 召唤兽的物种表、资质/成长区间、妖类境界正式命名、首批兽诀和寿命恢复道具数值。

这些属于 balance/content decision，不得反向破坏前述隔离、编译、版本和持久化边界。

---

## 17. 决策变更流程

后续若要改变本文中的已确认方向：

1. 在本文新增“决策记录”，写明日期、旧方案、新方案、原因和数据迁移影响。
2. 判断是否需要提升 `projectionVersion`、`contentVersion` 或 `rulesetVersion`。
3. 先补迁移和回放兼容策略，再修改生产代码。
4. 更新受影响测试和兼容性文档。

### 决策记录

| 日期 | 决策 | 影响 |
| --- | --- | --- |
| 2026-09-06 | 8A 由最小双人邀请切磋调整为复用现有擂台房间，保留每方最多四人 | 不新增邀请、准备或组队机制；多人控制与验收提前至 8A，8C 聚焦观战与负载 |
| 2026-09-06 | 下令包括倒地人物；超时和确认离线默认普攻，离场使用逃跑 | 替换原超时防御建议；修改提交、指令查询与行动队列语义，实施时提升相关战斗版本；不增加认输或弃权 |
| 2026-09-06 | 擂台停机维护后硬切 v6，不维护旧战斗过渡，不增加功能开关 | 维护期定向清理旧擂台临时运行态与索引，不删除历史归档；全量 v5 退役仍单独安排，多参与者归档迁移在 8A 实施中落实 |
| 2026-09-06 | Phase 7D 补齐独立 v6 战斗页面、二级技能指令、Tooltip 与逐行动差量播放 | 复用现有页面与展示协议进入 Phase 8；不将 UI 阵容容量视为多人或召唤兽交付 |
| 2026-09-06 | Phase 8 从整阶段范围拆为 8A 双人权威切磋、8B 回放播放器、8C 多参与者与观战 | 8A 采用满状态、无持久资源回写、无奖励纵切；首版提交即锁定、超时防御，后续持久模型与协议变化由专项设计决定，本轮无数据迁移 |
| 2026-09-06 | 在线传输从待选方案锁定为 HTTP 提交/恢复与 WebSocket 状态/回合结果推送 | WebSocket 不高频同步画面；React 与播放控制器逐行动消费服务端差量帧，具体消息与回放版本在实施前锁定 |
| 2026-09-01 | combat-v6 采用梦幻式 we-go 核心，与 battle-v5 完全隔离 | 新战斗内容全部以 v6 DSL/投影重建 |
| 2026-09-01 | 除角色基础事实外，装备、功法、宗门技能和经脉另起新体系 | 不建设旧 build 的长期翻译层 |
| 2026-09-01 | 修炼系统由现有炼体五轨原位改造，五条等级一对一继承，旧效果退出 | 新结构为四修炼 + 生命根基，需幂等迁移 |
| 2026-09-01 | 兼容期人物等级使用 36 阶段映射 5～180 | 后续正式公式通过 projection version 演进 |
| 2026-09-02 | 新装备系统定名“道装”，采用梦幻式六部位并使用修仙化术语 | “法宝”保留给未来独立系统 |
| 2026-09-02 | 道装不设品质、稀有度或权威评分，只展示实际数值和效果 | 生成、展示、市场与筛选不得重新引入替代品质 |
| 2026-09-02 | 道装完全独立且不使用境界衰减 | 建立独立生成/装配/投影领域，以器阶自然换代 |
| 2026-09-03 | Phase 4A 锁定器胚、附灵、九种阵法灵纹和完整投影顺序 | 发布 `dao_equipment_generator_v1` 与 `character_equipment_v1` |
| 2026-09-03 | Phase 4B 锁定七器蕴、九器诀、战意与 generator v2 | 发布 `character_equipment_special_v1`；持久化和经济拆至 Phase 4C |
| 2026-09-03 | 淬炼永久删除，道装首版固定已鉴定 | 领域、接口和未来路线均不保留淬炼或鉴器流程 |
| 2026-09-02 | 功法槽位为炼气 2 槽、逐大境界增加并在化神封顶 6 槽 | 后续境界不再增加槽位 |
| 2026-09-02 | 功法首版仅一套构筑、不固印、无 AI | 保留未来多构筑迁移边界，不提前实现切换和锁定能力 |
| 2026-09-03 | Phase 5A 锁定十谱系二十定义与完整人物投影 | 发布 `character_build_v1`；法连、连击与功法外围持久化不进入本阶段 |
| 2026-09-03 | Phase 6A 锁定多宗门注册表与幽都经典双流派 | 发布 `daoyou_rules_v2` / `character_build_v2`；固定伤害与伤势成为中立 core 原语 |
| 2026-09-04 | Phase 6D 锁定九劫天宫天律镇妖/九霄驭雷双流派 | 发布 `daoyou_rules_v5` / `character_build_v5`；旧劫雷、劫债、劫数与整技能免疫永久退出 v6 |
| 2026-09-04 | Phase 7C 锁定 Redis 权威运行态、通用终局事件与独立 v6 回放 | 训练房迁出进程内 Store；战斗不绑定任务且 PostgreSQL 只保存终局回放 |
| 2026-09-02 | 锁定 `character_panel_v1` 六维职责、裸身公式和面板点数单位 | 保留现有自然成长、自由加点与洗点；v6 独立重算派生面板 |
| 2026-09-02 | 宗门保留特色机制，特殊资源按需配置 | 无相形态、天衍五行、幽都固伤/削弱/伤势等以 v6 通用原语重建 |
| 2026-09-02 | 每宗门固定双流派，每流派七层且每层三个互斥节点 | 每宗门最终交付 42 个经脉节点 |
| 2026-09-02 | 两个流派各保存一套经脉方案，战斗外免费切换和重置 | 不提供同一流派多方案槽位，战斗使用锁定快照 |
| 2026-09-02 | 旧宗门心法等级和经脉深度平滑迁移，不返还修为灵石 | 按心法槽位、最高深度和节点位置继承，旧战斗效果退出 |
| 2026-09-02 | 心法上限为人物等级加 10，取消通用 growthProfile 与四技能装配 | 技能显式读取所属心法等级；统一普攻，快捷栏仅为 UI 偏好 |
| 2026-09-02 | 炼体五轨名称、等级、进度与丹药方向原值保留，只替换 v6 战斗作用 | 不建立直接点修或平行持久模型，继续通过 condition 消耗品链路推进 |
| 2026-09-02 | 肉身七阶保留为阶段位阶与单轨上限，晋升改为无消耗确定性点击 | 取消专项轨道要求、材料、概率、失败、保底和全部旧战斗被动 |
| 2026-09-02 | 锁定四修炼差与生命根基数值 | 物法差为 `2% + 5` 且限 ±20；封禁差限 ±10；气血轨最高提供裸身气血 30% 与治疗强度 30 |
| 2026-09-02 | 种族、灵根和命格首版全部不进入 combat-v6 | 不预埋空字段、标签或兼容效果；后续有明确需求时重新设计并提升相关版本 |
| 2026-09-02 | 召唤兽采用独立五维、五资质、成长与 180 级妖类境界映射 | 不复用人物六维或人物 build，首版不设闪避资质和召唤兽修炼 |
| 2026-09-02 | 召唤兽出生技能数量永久决定技能格上限，打书随机覆盖 | 不提供指定覆盖、固印、锁定、保底或后天增格 |
| 2026-09-02 | 玩家最多携带 6 只召唤兽，场上最多 1 只 | 编组与个体分离持久化，换宠消耗人物行动 |
| 2026-09-02 | 召唤兽死亡扣除 50 寿命，低于 50 禁止出战，归零不删除 | 由 Host 在战后事务中幂等结算，首版提供最小寿命恢复出口 |
| 2026-09-02 | 法宝首版不实现 | 作为未来独立构筑系统记录，当前不预埋数据库字段或 core 分支 |
| 2026-09-03 | Phase 3 采用红尘剑宗完整纵切，双流派为斩尘证道与万剑归一 | 交付六心法、42节点、剑意资源与 `character_sect_v1`，不接旧宗门进度或线上服务 |

## Phase 7D 接入状态（2026-09-06）

首个普通PVE采用青溪灵草坡野生灵兽暗雷：点击必遇敌、角色每日共享20次、固定区域物种与等级。已接入Redis遭遇运行态、独立Condition结算与v6地图页面；任务消费者、奖励、捕捉及宠物持久化仍为后续范围。详见 [Phase 7D专项设计](./combat-v6-phase-7d-wild-encounter-design.md)。基础设施与页面验收以专项稿及实施报告为准，不将纯逻辑通过等同于线上验收。

新版战斗页面、二级技能抽屉、独立 Tooltip、差量协议与逐行动播放已交付，认证野怪胜利和资源结算链路已回归。详细 UI 与验证范围见[战斗 UI 与展示协议](./combat-v6-battle-ui.md)。Phase 8A 已接入擂台及独立 v6 页面，并在隔离本地环境以八个真实认证账号完成 4v4 收令、推送、逃跑终局与归档。[Phase 8A 验证记录](./combat-v6-phase-8a-arena-online-design.md)中的故障与响应式检查仍需收口。经确认进入 Phase 8B，已接入新版战绩及回放，具体完成与未验收范围见 [8B 专项记录](./combat-v6-phase-8b-replay-design.md)。
