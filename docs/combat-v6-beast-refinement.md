# 归元灵露洗炼

2026-09-12，按用户确认方案实现。入口为灵兽详情「领悟传承」旁的「洗炼」，复用传承灵印抽屉、背包选择、确认弹窗和操作后刷新流程。首版只通过本地dev接口发放，无新增掉落或商店购买渠道。

## 规则与配置

策划配置：`src/shared/engine/combat-v6/beasts/data/refinement.json`，配套JSON Schema和`refinement-config.ts`校验。配置维护名称、图标、颜色、描述、适用境界、单次消耗、叠加上限、归零等级修为和寿命恢复开关。

两档灵露共用 `origin-dew` 瓶形图标，通过 `color` 区分：归元灵露为 `jade`（青碧），上品归元灵露为 `gold`（金色）。储物袋格子与物品详情的图标、名称同步使用该配色，颜色不影响洗炼概率。

| 定义ID | 名称 | 适用物种 |
| --- | --- | --- |
| `beast.refinement.origin-dew` | 归元灵露 | 炼气、筑基、金丹 |
| `beast.refinement.superior-origin-dew` | 上品归元灵露 | 全部游戏境界；元婴及以上必须使用 |

每次消耗一瓶。判断依据为物种携带境界，与灵兽当前等级无关；主人仍须达到物种携带境界。上品不提高任何概率，与普通使用同一`rollBeastTraits`函数、同一物种出生配置；低阶物种在相同seed下两种药露结果完全一致。

洗炼将等级、修为、已分配点和初始等级归零，身份变为宝宝并恢复50待分配点，保留变异；资质、成长、全部技能与格数重新生成。核心出生必带，原有传承灵印与融合额外技能不保留、不返还，超出生区间数值也会重新抽取。技能格可以增加或减少，始终等于新技能数量。0级灵兽可以重复洗炼，重新升级正常获得属性点。

当前寿命恢复至原有上限，寿命上限数值不变。保留个体ID、主人、名字、物种；不修改携带编组或首发。更新生成seed、算法版本、物种内容修订与个体revision。旧版本灵兽仅在主动洗炼时进入当前物种生成规则。

## 服务端与界面

`POST /api/combat-v6/inventory`新增`action: refine`，与传承灵印学习共用现有认证、角色归属、战斗占用检查、角色锁、数据库事务与资源事件。提交物品ID／revision、灵兽ID／revision；不接受客户端随机seed或结果。

道具必须在背包且数量足够，灵兽必须属于当前角色且revision匹配。药露扣除与个体写入同时提交，失败整体回滚。成功后物品和灵兽revision递增；重复旧请求因版本失效被拒绝，不会再次扣费或重抽。响应丢失时刷新灵兽与背包核对，不提供旧结果择优保留。

界面确认明确提示等级加点归零、全部技能重置、格数可能减少、传承灵印不返还、寿命恢复及不可撤销。成功提示技能格变化并刷新灵兽详情。药露作为固定库存物品注册，复用背包堆叠、储藏和已有物品分类；没有新增产出配置。

## 本地发放

沿用现有`POST http://127.0.0.1:3001/api/dev/resources`，仅纯本地环境开放，无需新增专用发放接口。替换为只读查询得到的本地测试角色ID：

```json
{
  "cultivatorId": "本地测试角色UUID",
  "grants": [
    { "type": "item", "item": { "definitionId": "beast.refinement.origin-dew", "quantity": 1 } },
    { "type": "item", "item": { "definitionId": "beast.refinement.superior-origin-dew", "quantity": 1 } }
  ]
}
```

## 验证

共享单测覆盖十五种物种的药露门槛、等级归零后门槛不变、普通与上品相同seed结果一致、0级重复洗炼、技能格增减、加点和修为归零、寿命归满且上限保留、旧版本个体、未知道具、主人境界校验与重新升级。

本地页面实测：通过dev发放两种灵露各一瓶及一只四高级技能测试青灵狐。普通灵露将其从10级／4技能洗为0级／2技能；上品灵露再对这只0级灵兽成功洗炼。两次均扣除一瓶，显示新资质、成长与出生技能。检查360像素确认弹窗，文案与按钮可见，控制台无错误。测试只操作新发放个体，保留原有四只灵兽；两瓶药露已消耗，新测试个体保留在本地灵兽袋中（ID `2f041f45-6fb4-4afe-bcad-b5237b7989db`）。

寿命从不足恢复满额、元婴门槛与旧请求拒绝分别由共享逻辑测试或代码检查验证，未声称完成这些路径的浏览器实测。无生产写入、无数据库迁移。

最终检查：`bun run test`全量160个文件、1214项通过；`bun run lint`、`bun run build`（前后端类型检查与构建）、`git diff --check`通过。构建保留已有大资源包提示。未执行数据库迁移或生产发布。
