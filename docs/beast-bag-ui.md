# 灵兽袋页面

`/game/beasts` 延续现有接口与规则：最多拥有 24 只灵兽，出战编组最多 6 只，首发最多 1 只。未加入编组的灵兽仍计入灵兽袋容量。未新增仓库、扩容、改名或洗练机制。

- 桌面左侧名册、右侧选中灵兽详情；移动端名册三列排列于详情上方。全部／出战编组筛选保留可见选中项，否则展示当前筛选的第一只。
- 编组筛选显示六个位置；点击空位返回全部灵兽，优先选中未编组灵兽，由玩家点击加入编组。
- 直接展示战斗面板上限、资质、成长、寿命、经验及基础属性。经验与面板取现有领域计算，无设计稿示例数值。
- 技能格复用 InkTooltip 的定位、触摸点击、键盘与关闭行为；只展示实际技能，被高级技能抑制时降低视觉强调并解释原因。
- 加点在属性面板内预分配，支持加号长按连加、重置及战斗属性预览，InkModal 二次确认后提交。传承灵印抽屉复用通用储物袋格位与物品预览，只允许选择传承灵印；休养与放生使用 InkModal 确认。请求反馈使用 toast；提交中阻止重复提交及关闭操作确认层。
- Tailwind v4 和现有纸色、墨色、teal、crimson 色标；常规操作使用 InkButton，不增加 CSS modules 或新状态库。

## 本轮验收（2026-09-09）

以下为初版验收；后续交互调整以文末补验记录为准。

环境：127.0.0.1:5174，本地道友1。现有灵兽为 11 级青灵狐，寿命 750/1000，待分配 5 点。

- 页面与路由标题显示灵兽袋；桌面双栏与 360px 上下布局已检查，无横向溢出。
- 取消首发、移出编组成功，计数同步、六个空位与空编组说明正确；点击空位后重新设为首发，已恢复原携带及首发状态。
- 加点输入法力 1，预览法力 275→278、法攻 178→180、法防 66→67；退出未提交，未消耗点数。
- 休养确认显示恢复 250 寿命、消耗 25 灵石；放生显示不可找回说明。均取消，未进行资源消费或放生。
- 技能格点击展示真实灵火说明，Escape 可关闭；窄屏浮层左 12px、右 300px，未超出 360px 视口。未独立验证真实触屏设备。
- 领悟传承入口打开专属流程，正确显示无传承灵印空态；本轮未进行真实学习消费。
- 浏览器未捕获 error/warn。未进行多只灵兽、满 24 只、满编组、断网及不可出战个体的完整运行验收；相关限制通过代码检查。
- 未修改领域规则，不新增或运行共享单元测试；执行 lint、build、触及文件 Prettier 与 diff 检查。

## 面板内加点与物品栏复用补验（2026-09-09）

- `AttributeAllocation` 为受控通用组件，不依赖灵兽领域；消费方提供属性、可用点数、草稿与确认入口，负责领域预览和提交。灵兽使用现有 `allocateBeast` / `beastPanel` 计算；个体、revision 或人物等级变化时重新建立草稿。
- 长按 350ms 开始每 80ms 加一点；释放、取消、失焦、页面隐藏和卸载清理计时，点数用尽禁用。确认弹窗期间锁定草稿，返回调整保留草稿；提交仍沿用 revision 与重复提交保护。
- `InventoryItems` 统一拥有真实格位、40 格容量布局与 ItemSlot 渲染，储物袋管理页和传承灵印学习抽屉共同使用。玩法只提供选择条件和预览操作，非传承灵印不能用于学习；选中后的消耗说明与确认按钮固定在抽屉 footer。
- 本地道友1：单击法力加号预分配1点，面板法力275→278、法攻178→180、法防66→67；鼠标按住后分配到5点即停止，所有加号禁用。二次确认列出法力+5、消耗5点；返回调整与重置正常，未实际提交，不消耗测试角色点数。
- 360px 下检查加点区、40格传承灵印选择抽屉和普通储物袋渲染；无横向溢出。非传承灵印物品弱化且无学习操作。当前没有传承灵印，未验证实际传承灵印选中／消费提交、加点实际提交及网络失败恢复；真实触屏长按未独立验证。
- 执行 lint、build、触及文件 Prettier 和 diff 检查；没有修改领域逻辑，不新增或运行单元测试。

## 角色加点复用（2026-09-09）

- `/game/cultivator/attributes` 通过轻量角色适配组件复用 `AttributeAllocation`，六维标签和当前值来自角色数据。去掉数值输入框，使用小加号、长按、草稿重置与 InkModal 二次确认；桌面六列，移动端两列。
- 预览六维基础属性，本页“当前属性详情”仍显示服务端已生效数据；提交沿用 `/api/cultivator/attributes/allocate` 和资源刷新机制。保留洗点道具流程，与加点提交互斥，使用同步锁避免重复请求。
- 本地道友1原剩余点数0，通过本地 dev 接口临时设为5，验证灵力50→51预览、确认弹窗、返回调整和草稿重置；检查360px布局后恢复0，刷新确认六维仍均为50。未实际提交加点或消费洗点道具，未重跑角色长按（沿用已验收通用组件）。
- 执行 lint、build、Prettier、diff 检查，无领域变更，未新增或运行单元测试。

## 视觉规范统一与补验（2026-09-17）

- 保留名册／详情布局、分区与技能名称的原有单行／缩字双行渲染逻辑。筛选复用 `GameSceneTabs`，首发／编组复用 `InkBadge`；高级与失效技能格使用项目色标。
- 出战与确认操作使用 `InkButton` 主变体，取消／移出／返回降低强调，改名使用 ghost；共享加点组件的确认／重置同步采用主次变体。成长、寿命、修为与容量计数补齐 `font-mono`。
- 禁用的首发、编组、喂养与休养入口旁提供独立 `InkTooltip`，支持点击与键盘；编组请求仅在触发按钮显示 pending，其余操作保持禁用。
- 本地 `127.0.0.1:5174`、本地道友6：桌面与360px布局检查通过，窄屏文档宽度为360px；筛选、空位选兽、加入／移出编组及徽记更新正常，已恢复原1只编组和原首发。改名弹窗打开后取消；预分配体质1点、进入确认、返回并重置，未提交改名、加点或资源消耗。
- 休养原因点击及键盘聚焦均显示“寿命已满”，Escape关闭；技能说明浮层在窄屏内显示。浏览器控制台未捕获warn/error。
- `bun run lint`、`bun run build`（含前后端类型检查）、触及文件Prettier与`git diff --check`通过。构建仍提示非本次文件的动态／静态导入混用与大包告警。无领域逻辑改动，未新增或运行单元测试。
- 当前角色无高级／失效技能，相关配色仅代码核对；未覆盖满编组、等级受限、网络失败或慢请求下pending的可见时长，也未做真实触屏验收。

## 名册高度、携带文案与朱砂技能（2026-09-18）

- 名册列表独立滚动，筛选与说明保留在滚动区外；桌面列表高度最多为视口的60%且不超过32rem，移动端最多15rem，侧栏不随详情拉伸。
- 面向玩家的“出战编组／加入编组／移出编组”统一为“携带出战／携带出战／取消携带”，同步徽记、空态、条件提示、操作反馈与场景描述，并简述携带召唤和首发入场的区别。
- 高级技能采用crimson朱砂文字、边框和淡底色；技能名称渲染保持不变。
- 本地道友6桌面与360px检查：筛选和空位正常；读取到桌面列表max-height为432px、窄屏为240px，overflow-y为auto，窄屏无横向溢出。当前仅2只灵兽，未实测满名册滚动与高级技能实景。
- 执行lint、build、Prettier及diff检查；纯展示改动未运行单元测试。

## 名册滚动视觉反馈（2026-09-18）

- 名册使用细墨色原生滑块，保留拖动、滚轮、触摸和键盘滚动；滑块长度与位置表达内容比例和当前位置。
- 仅在对应方向仍有内容时显示16px纸色渐隐，到顶／到底时移除对应边缘；短列表不显示滚动装饰。不添加滚动计数、状态说明或翻页按钮。
- 本地道友1现有19只灵兽实测：桌面可视高度432px、内容1220px，到底scrollTop为788px且下缘渐隐消失；360px可视高度240px、内容464px，无横向溢出。切换携带筛选后内容140px，无上下渐隐。未修改灵兽数据。
- `bun run lint`、`bun run build`、触及文件Prettier与`git diff --check`通过；浏览器无warn/error。未新增或运行单元测试，未验证真实触屏设备。

## 桌面名册与详情等高（2026-09-18）

- 桌面侧栏由右侧详情决定高度，筛选和说明下方的名册填满剩余空间；取消桌面60dvh／32rem上限，超出详情高度的名册仍内部滚动。空详情保留最小侧栏高度，移动端仍限高15rem。
- 本地道友1的19只灵兽实测：桌面名册可视高度756px，名册、侧栏和详情底部坐标一致；页面底部原空白被名册填充。360px下仍为240px列表高度，无横向溢出。
- lint、build、Prettier及diff检查通过；布局改动未运行单元测试。
